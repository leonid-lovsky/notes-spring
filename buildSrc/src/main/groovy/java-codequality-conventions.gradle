plugins {
	id 'java-javaformat-conventions'
	id 'java-errorprone-conventions'
	id 'java-jacoco-conventions'
	id 'java-jacoco-report-aggregation-conventions'
}

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(rootProject.file('.java-version').text.trim().toInteger())
	}
}
