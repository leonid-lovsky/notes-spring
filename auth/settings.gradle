pluginManagement {
    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'auth'

include ':application'
include ':webmvc'
include ':data-jpa'
