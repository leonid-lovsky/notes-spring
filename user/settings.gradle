pluginManagement {
    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'user'

include ':application'
include ':webmvc'
include ':data-jpa'
