pluginManagement {
    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'crud'

include ':webmvc'
include ':data-jpa'
