# CLAUDE.md — notes-spring

> Последнее обновление: Fri Sep 04 22:36:34 IDT 2026 **Всё временно** — любое решение подлежит обсуждению и изменению.

> **⚠️ Проект в процессе полного переписывания (начато пользователем 2026-08-14)** — состояние ниже смешивает зрелое «старое» описание (note/·user/·часть user-note/) с реальным на данный момент WIP. Детали и что именно уже изменилось — см. «Задачи» → «В процессе 2026-08-14».

Многомодульный Spring Boot 4 проект (`note/`, `user/`, `user-note/`, ...), реализующий hexagonal architecture единообразно во всех сервисах через Gradle convention plugins. Этот файл — точка входа: правила, текущая задача, активно пересматриваемые открытые решения. Вся необходимая для работы над проектом информация — здесь или в справочных файлах ниже, без обращения к внешним источникам. Формат оптимизирован для чтения/правки ИИ-агентом — см. «Правила» → «Документация — только для ИИ-агента, не для человека».

**Справочные файлы репозитория** (каталог `docs/`, читать по мере необходимости — не каждую сессию, в отличие от этого файла; `CLAUDE.md` — единственный документационный файл, остающийся в корне репозитория, обязательное условие автозагрузки Claude Code):
- `docs/file-catalog.md` — полный каталог файлов проекта со статусами `[DONE]`/`[REVIEW]`/`[ADD]`/`[REMOVED]` — текущая активная задача построчного пересмотра, см. «Задачи»
- `docs/decisions-log.md` — хронология принятых архитектурных решений (Архитектура/HTTP/Маппинг/Reactive/Convention plugins/Версии/Стиль кода)
- `docs/tech-glossary.md` — глоссарий внешних технологий, в основном ещё не используются (Kubernetes/Terraform/Kafka/OAuth2/Axon и т. д.)
- `docs/spring-boot-starters-reference.md` — полный список Spring Boot стартеров, эталон полноты для convention-плагинов (⛔ не сокращать)
- `docs/db-migration-tools-reference.md` — инструменты миграции БД по каждому driven-адаптеру
- `docs/google-docs-full-model.md` — набросок требований к полной модели совместного редактирования, см. «Открытые решения» → «Полная модель Google Docs»
- `docs/convention-plugins-graph.md` — снимок ТЕКУЩЕГО графа родитель→потомок всех convention-плагинов (не история решений — та в `docs/decisions-log.md`), читать при правке/добавлении convention-плагина вместо реконструкции графа по хронологии
- `docs/spring-boot-starters-full-matrix.md` — сырой снимок ВСЕХ 205 зависимостей Initializr (не куратированный список, в отличие от `docs/spring-boot-starters-reference.md`) по 4 комбинациям Boot 4.0.7/4.1.0 × Java 21/25, справочник общего кругозора, не источник для convention-плагинов
- `docs/spring-boot-testing-reference.md` — справочник тестовых уровней/аннотаций/классов Spring Boot 4.1 с датами появления (проверено по docs.spring.io/GitHub spring-projects, не Maven Central), собран для TDD-переписывания — читать при выборе конкретной тестовой аннотации/клиента

---

## Начало каждой сессии

1. Прочитать этот файл
2. Текущий приоритет — раздел «Задачи»
3. Актуализировать файл: убрать устаревшее, отразить новые решения
4. Коммит + пуш _(постоянная авторизация — искл. из правила «Коммиты» ниже)_

---

## Правила

- **Голый API-стартер не подключается, если есть обёртка (Spring Data и т. п.) для той же технологии — даже если технически обёртка не несёт 100% функционала голого стартера** (2026-08-15, явное решение пользователя, отменяет собственную рекомендацию агента по итогам факт-проверки) — прецедент: `spring-boot-starter-elasticsearch` подключён рядом с `spring-boot-starter-data-elasticsearch` (несёт реальный `elasticsearch-java`-драйвер, которого `data-elasticsearch` транзитивно не тянет, проверено и POM, и Gradle Module Metadata) — пользователь всё равно убрал голый стартер вручную и явно сформулировал общее правило: «не используем голый API при наличии обёртки». Риск (Spring Data Elasticsearch без реального клиента может не заработать в рантайме) осознан пользователем — «проверить при реальном обращении», не превентивным подключением про запас. Это отменяет более узкое обоснование из записи 2026-08-15 «пересматривает правило... но избыточность, доказанную фактами, применять не мешает» — теперь правило шире факта транзитивности: голый API не нужен как класс решений, независимо от того, доказана ли конкретная избыточность фактами по каждой технологии отдельно
- **⚠️ ИСТОЧНИК ИСТИНЫ ДЛЯ SPRING-ФАКТОВ — НЕ MAVEN CENTRAL ПОИСК** [НЕ СОКРАЩАТЬ/НЕ УДАЛЯТЬ — исключение из правила «убирать избыточность», даже при явной задаче уменьшить объём CLAUDE.md] (2026-07-14, резкая реакция пользователя на ошибку): вопрос «существует ли артефакт X» **никогда** не проверять через `search.maven.org` (Solr-индекс отстаёт от реального репозитория, `0 numFound` НЕ значит «не существует» — конкретная ошибка: заявил, что `spring-boot-starter-r2dbc` не существует по 0 результатам поиска, хотя это реальный отдельный от `data-r2dbc` стартер). Источник истины — **Spring Initializr** (`start.spring.io/metadata/client` — реальный список id/name/description), **GitHub spring-projects**, **официальная документация docs.spring.io**. Maven Central годится только для номера версии уже известного, существующего артефакта — не для вопроса о самом его существовании. **`docs/spring-boot-starters-reference.md`** (2026-07-14) — дословная копия со Spring Initializr, лежит в репозитории локально: для вопроса «существует ли стартер X» и «как называется» обращаться сначала туда, не в web-поиск. При выборе/добавлении стартера под конкретную технологию — сверяться по этому файлу, есть ли для неё отдельный `-test`-компаньон (`spring-boot-starter-{tech}-test`), и подключать его вместе с основным, если существует — не пропускать по своему усмотрению (см. прецедент с `spring-boot-starter-r2dbc`/`-jdbc`/`-elasticsearch`, где `-test`-версия изначально была упущена). Также перед добавлением зависимости в Spring Boot модуль — сверяться, нет ли для этой технологии уже готового `spring-boot-starter-*`: если есть, подключать именно его, а не «сырую» внешнюю зависимость напрямую (starter уже несёт нужный BOM-managed набор транзитивных зависимостей и autoconfiguration — обходить его напрямую значит терять то и другое). **Принадлежность к Initializr — определять только буквальным поиском строки артефакта в `docs/spring-boot-starters-reference.md`, никогда рассуждением по форме имени артефакта** (2026-08-02, резкая реакция пользователя на ошибку): конкретная ошибка — заявил, что `org.springframework.boot:spring-boot-docker-compose` «не Initializr-стартер», по неверной логике «раз в имени артефакта нет `-starter-`, значит это core-модуль Boot, а не выбираемая в Initializr зависимость» — вывод по паттерну имени, без единой проверки. У Spring Initializr многие зависимости (Docker Compose Support, Lombok, Actuator и др.) не содержат `-starter-` в имени артефакта, наличие/отсутствие этого подслова в имени не говорит ничего о членстве в Initializr. Строка `developmentOnly 'org.springframework.boot:spring-boot-docker-compose'` при этом дословно уже присутствовала в `docs/spring-boot-starters-reference.md:178` — файл был не прочитан перед утверждением. Правило: любое утверждение вида «X не относится к Initializr»/«X не существует как стартер» — сначала grep по точной строке артефакта в `docs/spring-boot-starters-reference.md`, и только отсутствие там даёт право на такое утверждение (с оговоркой, что файл не абсолютно полон — см. соответствующий пункт правила выше)
- **Язык** — общение на русском; **код и любые настройки проекта — исключительно на английском, без исключений** (не только идентификаторы, но и строковые литералы/сообщения об ошибках внутри конфигурационных файлов — прецедент 2026-08-29: два сообщения `RegexpMultilineCheck` в `gradle/checkstyle/checkstyle.xml` были на русском, найдены по явному запросу пользователя проверить весь репозиторий на кириллицу, оба исправлены/удалены в той же сессии)
- **Файлы** — не изменять без явного указания; перед созданием нового файла изучить проект
- **Актуальность `docs/file-catalog.md`** — при любом создании/удалении/переименовании файла в проекте синхронизировать `docs/file-catalog.md`: новый файл — строка со статусом `[REVIEW]`, реально удалённый — `[REMOVED]` (строка сохраняется для истории), переименованный/перемещённый — обновить путь. Делать сразу как часть той же правки, не откладывая до отдельного запроса; сами правила допустимых изменений каталога — см. «Лимит размера файла» ниже
- **Коммиты** — не коммитить без явного запроса, кроме начала сессии (см. «Начало каждой сессии», п. 4); «Зафиксируй» = обновить CLAUDE.md и дату → коммит → пуш
- **Дата перед коммитом** — брать из `date +"%a %b %d %H:%M:%S %Z %Y"` (локальное время, формат как в `gradle-wrapper.properties`), не выдумывать
- **⚠️ Роль ассистента в текущей итерации переписывания (закреплено 2026-08-19, повтор явного указания 2026-08-18)** — реализацию (правки Java/Gradle/тестового кода) выполняет только пользователь; ассистент — совет/консультация/факт-чек, не пишет и не редактирует код без отдельного явного запроса на конкретную правку. Документацию (`CLAUDE.md`/`docs/`) и git-операции (коммит/пуш) по прямому запросу — по-прежнему выполняет ассистент, это не «реализация». Действует до отдельной отмены пользователем, не факт, что постоянно
- **CI** — не изменять `.github/workflows/` без отдельного запроса
- **Списки вместо таблиц** — структурированные данные (путь/статус/комментарий и т. п.) — плоским списком `- поле — поле — поле`, не markdown-таблицей: правка одной записи не требует пересчёта соседних строк
- **Без ручных переносов строк** — один абзац/пункт/строка blockquote = одна строка файла, без жёсткого переноса на 80–120 символов; перенос только смысловой
- **Документация — только для ИИ-агента, не для человека** (закреплено 2026-07-24, реструктуризация `docs/` под это правило — см. `docs/decisions-log.md`): единственный реальный потребитель `CLAUDE.md` и всех файлов `docs/` — ИИ-агент, читающий сырой текст целиком за один проход, не человек, листающий GitHub UI, и не программа, парсящая файл по схеме (ни один скрипт эти файлы сегодня не парсит — см. «Открытые решения» → «Симметрия ArchUnit»). Отсюда конкретно:
  - путь в строке каталога может опираться на ближайший заголовок раздела (`#### {путь-к-директории}/`) вместо повтора полного пути от корня в каждой строке (пересмотрено 2026-08-02 — было «полный путь в каждой строке, самодостаточность без заголовков», отменено по прямому указанию пользователя; заголовок раздела физически стоит прямо над своими строками в том же файле, повтор в каждой строке избыточен)
  - ≤ 2 уровня вложенности заголовков (`### сервис/` → `#### модуль/`)
  - ASCII-статусы вместо emoji (кроме намеренных маркеров важности `⚠️`/`⛔`/`⚡`)
  - явная схема полей перед списком
  - плоский список `- поле — поле — поле` вместо markdown-таблицы **и** вместо ASCII-таблицы на ручном выравнивании пробелами (тот же порок — правка одной записи требует пересчёта отступов у соседних)
  - один абзац/пункт/строка — одна физическая строка файла, без ручного переноса на 80–120 символов (перенос строк — забота текстового редактора человека, не автора файла)
  - никаких элементов, ценных только человеку и бесполезных агенту, читающему файл целиком за один проход (кликабельное оглавление с `#якорями` — убрано 2026-07-24, агент не кликает ссылки)
  - вместо «см. выше»/«см. ниже»/«ранее» — конкретный якорь (точное название раздела или дата), чтобы находка по grep была самодостаточной без сканирования соседних абзацев
  - для файлов-справочников (`docs/file-catalog.md`, `docs/spring-boot-starters-reference.md` и т. п.) — предпочитать связную прозу/плоский список перед жёсткой схемой (JSON/YAML/CSV): агент читает текст семантически, а не через парсер, жёсткий формат добавляет токены на скобки/кавычки без выигрыша, раз ничего не парсит файл программно, и вырезает прозу с обоснованием «почему», которая для агента ценнее самой структуры
  - абсолютные даты вместо относительных выражений времени («вчера», «на прошлой неделе»)
- **Архитектурные решения** — сначала варианты с плюсами/минусами и рекомендацией, дождаться выбора пользователя; не реализовывать до явного решения
- **Перед архитектурным ответом — контрольный список: стандартный паттерн / переформулировка / другой угол / альтернативы** (2026-08-08, обобщено по итогам диалога про единый контракт Controller/Service — см. «Задачи» → «Завершено» → «2026-08-08, единый generic-контракт Controller/Service/Repository» и `docs/decisions-log.md` → «Архитектура», где ассистент сначала трижды прошёлся по локальным вариациям (Object-хак / отказ от интерфейса / снятие ограничения), прежде чем распознать типовой случай): прежде чем предлагать вариант решения, явно проверять четыре вопроса, не бросаться к первому найденному через локальный код рецепту — (1) не является ли задача частным случаем известного стандартного паттерна (пример прецедента — «N классов с совпадающими по сигнатуре, имя+параметры, JLS, методами, различающихся типом возврата» → параметрический полиморфизм, как `Optional<T>`/`Comparator<T>`/`CrudRepository<T, ID>`); (2) можно ли переформулировать саму постановку задачи, не размывая заданных пользователем ограничений; (3) можно ли посмотреть на неё с другого угла — начинать рассуждение с абстрактной формы задачи, а не с того, что уже реализовано в репозитории как локальный прецедент; (4) есть ли альтернативные способы решения помимо первого пришедшего в голову. Решения, которые снимают заданное пользователем ограничение ради тривиальности (прецедент — предложение убрать `ResponseEntity` из Controller), предлагать можно, но явно помечая как «меняет постановку задачи», не выдавая за рекомендованный ответ на исходный вопрос. **Чек-лист проходить явно и видимо в самом ответе** (уточнено 2026-08-08, повторный запрос пользователя зафиксировать «способ размышления»; форма уточнена 2026-08-21 — резкая реакция на буквальные ярлыки-вопросы вида «Стандартный паттерн? Да —») — не как молчаливая внутренняя проверка перед выводом готового решения, а как показанные шаги рассуждения, поданные обычной связной прозой, без ярлыков/риторических вопросов-заголовков на каждый пункт чек-листа — форма чек-листа (четыре вопроса) держится в голове при рассуждении, в тексте виден только его результат, естественным языком; при повторном применении к уже решённому вопросу — не просто повторять прежний вывод, а переискать его заново (пример — единый контракт Controller/Service передуман через рамку Adapter/GoF и уже принятую в проекте driving/driven-симметрию портов, а не просто повторно назван «параметрический полиморфизм»). **Свежий взгляд, не фиксация на уже предложенных вариантах** (уточнено 2026-08-08, третий подряд запрос пользователя зафиксировать способ размышления) — при создании решения смотреть на проблему заново, не ограничиваясь набором вариантов, уже прозвучавших в диалоге (A/B/C/D/F и т. п.) — этот набор не исчерпывающий по умолчанию, промежуточный список — не потолок для дальнейшего поиска
- **Запрещены заглушки как решение задачи** (2026-08-08) — заглушка (placeholder, TODO-затычка, урезанная версия «пока сойдёт») не решает проблему, а избегает её; решение всегда рабочее
- **Запрещён откат к более примитивному варианту ради простоты реализации** (2026-08-08) — если выбранное решение оказывается сложным в реализации, разбираться со сложностью, не понижать задним числом планку уже выбранного варианта — это тоже избегание проблемы, не решение
- **Если проблема возникла — у неё есть причина, и аналогичная возникнет снова** (2026-08-08) — решение должно закрывать причину, а не только конкретное проявление: искать и устранять тот же класс проблемы в остальных местах проекта, где действует та же причина, не ограничиваясь местом обнаружения (прецедент — единый контракт Controller/Service решён не только в `user-note`, где вопрос возник, но и в `note`/`user`, где действовал тот же `Object`-паттерн, см. `docs/decisions-log.md` → «Архитектура» → «Трёхуровневая иерархия портов: `Object`-ковариантность → generic-параметризация»)
- **Пересмотр решений** — статус «принято»/«зафиксировано» (в т. ч. в `docs/decisions-log.md`) не закрывает вопрос навсегда: любое решение по CRUD-сервисам и по проекту в целом нужно поднимать заново для обсуждения и пересмотра в подходящий момент, а не только по отдельному запросу
- **Кольцевые/ромбовидные зависимости — ВСЕГДА избегать, без исключений, в любом месте проекта** (введено 2026-07-14, ужесточено 2026-07-15, **сделано абсолютным 2026-08-01**): при любой правке convention-плагинов, Gradle project-зависимостей (`projects.*`) или Java-иерархий портов/адаптеров — проверять граф на циклы и на схождение путей (диамант «вида 1» — определение см. в этом же пункте, «Два разных вида схождения — не путать») и устранять его, даже ценой дублирования кода — чистота/ясность/предсказуемость графа важнее объёма copy-paste. Это отменяет более раннюю формулировку «решение оставить/устранить — за пользователем»: пользователь дважды экспериментировал с осознанным принятием диаманта ради устранения дублирования (`spring-cloud-application` дважды — 2026-07-15 и 2026-08-01) и оба раза в итоге откатывал это решение обратно к нулю диамантов — правило зафиксировано на основе этого повторяющегося исхода, не отдельного случая. Сообщать о находке по-прежнему обязательно (что найдено, где), но действие по умолчанию теперь «устранить», а не «спросить». Прецедент устранения без дублирования, когда возможно: `spring-cloud-application` (диамант `spring-cloud`+`spring-boot-application`) устранён заменой на прямой `id("org.springframework.boot")`, так как `spring-boot-application` к тому моменту стал чистым alias без собственной нагрузки — 0 диамант, 0 дублирования. Прецедент устранения ценой дублирования: `spring-boot-actuator` — отдельный атомарный плагин, применяется явно `id(...)` на каждом из 37 bootable-листовых модулей (не встроен вторым родителем ни в `spring-boot-application`, ни в `spring-cloud-application`, хотя это устранило бы необходимость 37 явных `id(...)` ценой диаманта — отклонено). Никаких shared-функций/helper-кода в `build-logic/` для устранения подобных схождений — только композиция через `id(...)`. **Два разных вида схождения — не путать**: вид 1 — диамант внутри самого convention-плагина (файл в `build-logic/` сам в своём `plugins{}` вызывает 2+ `id("com.example.*")`, сходящихся в общем предке) — устранять всегда, без исключений. Вид 2 — листовой модуль (`build.gradle.kts` сервиса) напрямую применяет 2+ convention-плагина, сходящихся в общем предке (например `note/application-h2-jpa` — `spring-boot-application`+`spring-boot-database-h2`; `gateway/application` — `spring-cloud-application`+3 `spring-cloud-*`) — **не диамант для устранения**, а осознанная композиция независимых convention-плагинов в точке использования, ради которой вся система convention-плагинов и построена (решено пользователем 2026-07-15, не поднимать заново без нового явного повода)
- **Convention-плагины — всегда атомарны, один плагин = один концерн/технология** (закреплено как постоянное правило 2026-08-01, до этого — серия точечных решений, см. `docs/decisions-log.md` → «Атомизация базовых плагинов»): не объединять в одном плагине несколько независимых зависимостей/стартеров ради удобства, даже если они сегодня всегда применяются вместе (прецедент — вынос `spring-boot-starter-validation` из `spring-boot` в отдельный `spring-boot-validation`; воссоздание `spring-boot-actuator` из `spring-boot-application`). Если возможность применяется не везде одинаково или концептуально независима — отдельный плагин, композиция через явный `id(...)` в точке использования, не через объединение содержимого
- **Предпочитаем явную композицию неявному наследованию — во всём проекте, не только в convention-плагинах** (закреплено 2026-08-01, обобщает предыдущий пункт и правило про диаманты — все три работают вместе на один результат; область действия расширена пользователем в тот же день с Gradle-плагинов на весь проект целиком): когда модулю/классу/конфигурации нужны 2+ независимые возможности — явное объявление каждой рядом, в точке использования, а не одна возможность, встроенная родителем/зависимостью другой ради экономии строк. Действует одинаково в трёх плоскостях:
  - **Convention-плагины** (где правило впервые сформулировано) — явный `id(...)` для каждой возможности на листе, не встраивание через родителя. Даже если возможность нужна на 100% модулей, где применяется родитель, без единого исключения (как у `spring-boot-actuator`) — это не повод встраивать её через наследование. Отход от композиции — не ради компактности кода, а только когда явной альтернативы физически не существует (прецедент: `spring-boot-docker-compose` — `id("org.springframework.boot")` там не встраивание ради экономии, а техническая необходимость для typed-аксессора `developmentOnly`)
  - **Java-код** — предпочитать композицию наследованию классов везде, где выбор есть (внедрение зависимости/делегирование вместо `extends` ради переиспользования кода); интерфейсная иерархия портов (`{Entity}Interface`→`{Entity}ServiceInterface`/`{Entity}ControllerInterface`, см. «Именование») — контракт, не реализация, этого правила не касается
  - **Настройки/конфигурация** (Spring `application.properties`, профили и т. п.) — явные, отдельные свойства/файлы на концерн, не неявное наследование конфигурации через профили-предки или глубоко вложенные default-механизмы

  Естественное следствие для всех трёх — диамантов/скрытых схождений не возникает вообще, потому что диамант — всегда результат наследования от 2+ путей, а не композиции
- **`.gitattributes` — всегда точная копия шаблона Spring Initializr, ничего сверх неё** (закреплено 2026-08-01): `/gradlew text eol=lf`, `*.bat text eol=crlf`, `*.jar binary` — три строки, без `* text=auto` и любых других добавлений, даже если локальный `core.autocrlf=true` из-за этого выдаёт предупреждения `LF will be replaced by CRLF` при `git add` (сами предупреждения игнорировать — не повод отступать от стандартного шаблона; правильное место фикса — локальный `git config` конкретного контрибьютора, не общий для репозитория файл)
- **Только convention-плагины — листовые модули не применяют ничего напрямую** (проверено и закреплено 2026-07-15): ни один из 39 листовых `build.gradle.kts` (`note`/`user`/`user-note`/`auth`/`registry`/`config`/`gateway`) не должен применять plugin id, не начинающийся с `com.example.*`, и не должен объявлять зависимость иначе как `implementation(projects....)`/`api(projects....)` (project-to-project). Все внешние зависимости, плагины, версии и настройка сборки — только через `build-logic/convention/`. Проверка: `grep -rn 'id("' --include="build.gradle.kts" note user user-note auth registry config gateway | grep -v 'id("com\.example\.'` и аналогично по `dependencies{}`-блокам — оба должны быть пустыми. Прогонять эту проверку при добавлении новой зависимости/плагина в любой листовой модуль
- **После широкого `git checkout`/отката, затрагивающего convention-плагины — сверять число применений грепом, не только зелёный `clean check`** (2026-08-02, найдена и исправлена реальная регрессия — см. CLAUDE.md → «Задачи» → «Завершено 2026-08-02»): `id("com.example.spring-boot-actuator")` был случайно стёрт с 37 листовых модулей широким откатом диамант-эксперимента (`git checkout` затронул больше файлов, чем предполагалось), `./gradlew clean check` при этом оставался зелёным — отсутствие неиспользуемого стартера не ломает компиляцию, только полноту функциональности. После любого `git checkout <commit> -- <files>`/массового отката в `build-logic/` или в `plugins{}`-блоках листовых модулей — сверять `grep -rl 'id("com.example.{id}")' note user user-note auth registry config gateway | wc -l` для каждого затронутого плагина против чисел применений, заявленных в `docs/convention-plugins-graph.md`, не полагаться только на факт успешной сборки
- **Плагины-расширители (`java-library` и подобные) — только там, где реально нужна расширенная конфигурация, не каскадом «на всякий случай»** (введено 2026-07-15): прецедент — `com.example.jspecify` подключал `id("java-library")` только ради `api("org.jspecify")`, и через каскад `base → codequality → jspecify` эта конфигурация просачивалась во все 39 модулей, хотя видимость зависимости на потребителях от `api`/`implementation` не зависела (jspecify достаётся каждому модулю напрямую через собственный каскад `id(...)`, а не через project-to-project зависимость, где `api` реально что-то протаскивает дальше). Исправлено на `id("java")` + `implementation(...)`; `java-library` остался только в `com.example.java-library` (переименован из `com.example.library` 2026-08-01 — id совпадает с именем обёрнутого ядрового Gradle-плагина, см. `docs/decisions-log.md`), применяемом 6 модулями `contract`/`contract-reactive`, где `api(projects.*.domain)` — настоящая публичная сигнатура. При добавлении/правке convention-плагина, если он подключает id, расширяющий базовый (`java-library` поверх `java` и аналоги) — проверять: (1) нужна ли расширенная конфигурация именно этому плагину для собственной декларации зависимостей, или сводится к базовой; (2) не полагаться на то, что зависимость «протекает» api-путём к потребителям, если фактическая топология — каскадное применение `id(...)` внутри одного проекта, а не project-to-project. Проверка: `grep -rln 'id("java-library")'` по `build-logic/convention/` — список плагинов должен совпадать со списком, где реально объявлен `api(...)`
- **Build** — после каждого логического шага: `./gradlew clean check` (быстрее `build`: без `assemble`/`bootJar`) → обновить CLAUDE.md → коммит → пуш; перед коммитом дополнительно `./gradlew clean build` (проверяет и паковку — `bootJar`/`resolveMainClassName`, что `check` не покрывает)
- **⚠️ Sandboxed/remote-агент окружение может не уметь запустить сборку вообще** (2026-07-24, обнаружено при работе через Claude Code on the web; **частично решено** в тот же день, см. апдейт ниже): проект требует Java 25 (`.java-version`), но в некоторых remote execution environment физически установлена только JDK 21, а автозагрузка нужного тулчейна через Gradle toolchain resolver `foojay` (`api.foojay.io`) заблокирована сетевой политикой окружения (`CONNECT tunnel failed, 403` — так же падает и прямой `curl` на `api.foojay.io`/`start.spring.io`, не только Gradle). Симптом — `./gradlew` падает на **любой** задаче, требующей toolchain (`compileJava`, `dependencies`, `check`, `build`), включая полностью нетронутые модули (`note:domain` и т. п.) — это диагностический признак, что проблема в окружении, а не в конкретной правке. Перед тем как делать вывод «правка сломала сборку» — обязательно проверить `./gradlew :{нетронутый-модуль}:compileJava` отдельно, прежде чем списывать на среду.
  **Апдейт 2026-07-24**: в части таких окружений JDK 25 всё же можно поставить в обход `foojay` — `archive.ubuntu.com` (в отличие от `api.foojay.io`/`start.spring.io`) не заблокирован сетевой политикой, пакет `openjdk-25-jdk-headless` стоит напрямую через `apt-get install`, Gradle находит его сам через стандартное сканирование `/usr/lib/jvm` без правки `JAVA_HOME`/конфигов. После установки `./gradlew clean check` реально прошёл (151 из 154 задач, см. следующий пункт про Testcontainers/Docker Hub). Это специфично для конкретного окружения (сетевые allow-list различаются) — если `apt-get update` тоже не проходит или пакета нет в кэше, тогда билд-верификация действительно недоступна, откладывать её до среды с сетевым доступом (локальная машина пользователя, CI), коммитить по явному решению пользователя без нее, а не блокировать работу
- **⚠️ Testcontainers может не работать даже с установленным и запущенным Docker-демоном** (2026-07-24): в отличие от JDK (см. «⚠️ Sandboxed/remote-агент окружение может не уметь запустить сборку вообще» выше), скачивание образов с Docker Hub (`production.cloudfront.docker.com` — CDN, отдаёт сами слои/блобы) заблокировано сетевой политикой окружения — **осознанное решение egress-политики, не чинить и не обходить** (см. `/root/.ccr/README.md` → «403/407 от прокси»: «Do not retry or route around it — report the blocked host»). Симптом отличим от «демон не запущен» (`DockerClientProviderStrategy`/`ContainerFetchException` без деталей) — здесь дальше по стеку `DockerClientException` именно на этапе `PullImageResultCallback`, манифест образа успевает получить, блокируется скачивание блоба (`403 Forbidden` при прямом `docker pull`). Затронуты только модули с Testcontainers-тестами на реальную БД (`application-mongodb`, `application-{jdbc,jpa,r2dbc}-{mysql,postgresql}` — 7 модулей на сервис); H2-варианты (без Testcontainers) и весь остальной код не затронуты. Проверено эмпирически: `./gradlew clean check` — 154 задачи, 151 прошли, ровно 3 упали (по одному представителю на сервис `note`: `application-mongodb`, `application-jdbc-mysql`, `application-jdbc-postgresql`) — не баг кода, откладывать верификацию этих конкретных тестов до среды с открытым доступом к Docker Hub. **Отдельный, не связанный с сетевой блокировкой флейк — Docker Desktop только что запущен, `docker info` уже отвечает успехом, но внутренние подсистемы (сеть контейнеров, VM, прогрев) ещё не стабилизировались** (2026-08-02, локальная машина пользователя, Docker Desktop только что запущен командой): 3 задачи (`user-note` — `application-mysql-jdbc`/`-jpa`/`-r2dbc`) упали с `NoSuchFileException`/`EOFException` на файле результатов Gradle test executor'а (не assertion внутри теста — сам JVM test worker не успел корректно завершиться) в первом прогоне `clean check --continue` сразу после старта Docker Desktop. Причина не параллелизм Gradle — `application-mysql-r2dbc` падал тем же образом дважды подряд даже в одиночном прогоне (`./gradlew :user-note:application-mysql-r2dbc:test`, без остальных задач). Все 3 стабильно прошли при повторном прогоне через несколько минут после старта Docker Desktop, второй полный `clean check --continue` — 0 падений. Вывод: критерий готовности `until docker info` (ждать, пока демон начнёт отвечать) недостаточен для Testcontainers-тяжёлых сборок сразу после запуска Docker Desktop — API демона поднимается раньше, чем стабилизируются подсистемы, нужные Testcontainers. Не баг кода и не билд-логики, фикса на уровне репозитория нет — при запуске Docker Desktop специально под `clean check` закладывать паузу на прогрев (несколько минут) до тяжёлых параллельных Testcontainers-прогонов, не полагаться только на успешный `docker info`
- **⚡ `clean check` с реальным Docker — замер времени по вендорам** (2026-07-24, локальная машина пользователя с рабочим Docker Desktop, не sandboxed-окружение из пункта выше; повод — вопрос пользователя «почему `clean check` стал так долго выполняться») — `./gradlew clean check --continue --profile`: **1m48s wall-clock**, зелёная сборка (`org.gradle.parallel`+`caching`+`configuration-cache` уже включены в `gradle.properties` — суммарное время задач 13m6.88s сжато параллелизмом ~7×, дальше ускорять нечем на уровне Gradle-настроек). Раскладка `--profile`-отчёта по вендорам (вес почти целиком в `:test`-задаче composition-root модуля — компиляция/checkstyle везде доли секунды): H2 (9 модулей, без контейнера) — 9–14s; PostgreSQL (9) — 11–27s; MongoDB blocking+reactive (6) — 15–24s; **MySQL (9) — 35–57s, стабильно ~2× медленнее PostgreSQL/MongoDB** — согласуется с фактом различия wait-strategy из пункта выше (`MySQLContainer` — JDBC-based, требует реального подключения драйвером; `PostgreSQLContainer` — log-based, дешевле). Слой-модули (`domain`/`contract`/`data-*`/`webmvc`/`webflux`) и скелетные сервисы — субсекундные: рост числа composition-root модулей (15→33, см. «Вендор БД — граница Gradle-модуля» в `docs/decisions-log.md`) сам по себе не создаёт заметной нагрузки, весь вес — в 24 контейнеризированных `:test`-задачах (Testcontainers возвращены симметрично по вендорной оси, см. «Завершено 2026-07-23» в «Задачи»). Цифры информируют открытый вопрос «Стоимость масштабирования composition-root матрицы» ниже — конкретно вариант (б) того пункта
- **Подзадачи** — крупную задачу разбивать на подзадачи; коммитить и пушить после каждой завершённой подзадачи, чтобы не терять прогресс при обрыве сессии
- **⚠️ Лимит размера файла — при превышении сокращать самостоятельно, без напоминания** (2026-07-14, актуализировано 2026-07-14 по прямому запросу пользователя — три ограничения ниже закреплены здесь именно затем, чтобы не повторять их в каждом запросе «сократи файл»): триггер — сообщение `⚠ CLAUDE.md is over the 150.0k-char limit`. Мерить `wc -m CLAUDE.md`, **не** `wc -c` — тот считает байты UTF-8, а из-за кириллицы (2 байта/символ) байты почти вдвое больше реального числа символов, на котором основан лимит (проверено эмпирически 2026-07-14). Три вещи трогать нельзя, только переформулировать:
  1. Раздел «Правила» — ни одна инструкция не теряется по смыслу; формулировку каждого пункта можно сжимать (кроме отдельно помеченных исключений, см. правило про Maven Central выше)
  2. `docs/spring-boot-starters-reference.md` (вынесен из CLAUDE.md 2026-07-14, см. «Справочные файлы репозитория» в начале файла) — сам список зависимостей (и сам gradle-код) не сокращать и не удалять из него строки; допустима только проверка на дубликаты. Пояснительный текст вокруг списка и в CLAUDE.md — редактировать можно
  3. `docs/file-catalog.md` (вынесен из CLAUDE.md 2026-07-14) — список файлов и их статусы ([DONE]/[REVIEW]/[ADD]/[REMOVED]) не менять и не удалять по инициативе агента (можно добавлять пропущенные файлы `[REVIEW]`, можно убирать строки реально удалённых файлов, `[DONE]` — только по явному указанию пользователя); комментарий к каждому файлу — можно свободно сокращать вплоть до полного удаления
  Всё остальное в CLAUDE.md (в первую очередь «Задачи»/«Открытые решения» — исторические подробности) сокращать без ограничений, не теряя решения по существу. Та же логика (сокращать свободно, не теряя решения по существу) применяется к `docs/decisions-log.md`/`docs/tech-glossary.md`, если один из них сам разрастётся — независимо мерить `wc -m` по каждому файлу

---

## ⚡ Задачи

**⚠️ Текущий приоритет (2026-08-14) — полное переписывание проекта, начатое пользователем** — оба потока ниже **приостановлены**, не продолжать без отдельного запроса: `docs/file-catalog.md` не отражает состояние диска (не актуализировался под переписывание), «зрелость» как цель бессмысленна, пока меняется сама структура composition-root модулей. Оставлены как история/задел на потом:
1. Построчный пересмотр «Каталог файлов проекта»: ровно один файл за раз — разбор → утверждение пользователем → `[DONE]` → следующая строка `[REVIEW]` сверху вниз. Не пакетами, не забегая вперёд
2. **Зрелость CRUD-модулей** — цель: `note/`·`user/`·`user-note/` полностью зрелые и завершённые «со всех сторон». Скелетные сервисы (`registry/`·`config/`·`gateway/`·`auth/`) вне фокуса до отдельного запроса. Главная проблема на пути к цели — поведенческая аналогичность адаптеров/драйверов по матрице 5 технологий × вендор (см. «Открытые решения» → «Поведенческая аналогичность адаптеров/драйверов»). Плюс нерешённые грани: `spring-boot-starter-validation`, `@Transactional` на адаптерах, `findByEmail`/`findByUsername` без HTTP-входа, порядок методов, Javadoc, разрастание composition-root матрицы, обработка ошибок на HTTP-границе; unit-тесты бизнес-логики закрыты как отдельный пункт 2026-08-18 (см. ниже — не нужны отдельно от application-уровня); наблюдаемость по адаптерам — не начата

**⚠️ Обгоняющий шаг 2026-08-29 (вечер)** — пользователь не пошёл по 16-подзадачной разбивке ниже: вместо «слить JDBC+JPA через `@Profile` в одном модуле» удалён весь гексагональный слой `user-note/` (а также сервисы `note/`/`user/` целиком) и созданы **5 плоских модулей-скелетов** без `dependencies {}`/адаптеров, все в сборке и зелёные: `application-jdbc`, `application-jpa`, `application-mongodb` (sync); `application-r2dbc`, `application-mongodb-reactive` (reactive). Ось jdbc/jpa — снова граница модуля, не `@Profile`. Актуальное состояние — «Активная нить» → «2026-08-29 (вечер)» и «Статус по сервисам»; актуальный план — «Запланировано на следующие сессии» ниже.

**Запланировано на следующие сессии (обновлено 2026-09-04, вечер) — порядок зафиксирован пользователем: ИДЁМ СВЕРХУ ВНИЗ, начиная с composition-root**. План объединения JDBC+JPA через `@Profile` — снят.

**⚠️ Пересмотр 2026-09-04 (вечер) — отдельный шаг «сначала вывести абстракцию» снят, слит с TDD** — пользователь сам поднял вопрос («абстракция для application не имеет смысла» / «дублирует user flow, но нет straightforward способа решения»). Разбор: абстрагировать сейчас нечего — ни порта, ни домена, ни адаптеров не существует (весь слой удалён 2026-08-29). То, что дублируется в 5 модулях прямо сейчас (проверено по хешу — `UserNoteApplication.java`/`package-info.java` байт-в-байт идентичны, `UserNoteTestApplication.java` почти идентичны) — чисто структурная Spring Boot-церемония, уже принятая как неустранимая (см. «Завершено» → «2026-08-18, архитектура тестирования» — «структурное дублирование... не устраняется, требование самого Spring Boot»). Семантического дублирования (реального сценария/поведения) вынести не из чего. Вывод: `application-core`/порт/convention-плагин — не предварительный шаг, а естественный побочный эффект TDD (см. п.1 ниже, было п.0+п.1) — появится сам, когда появится реальный тест на реальный порт. Прежний план п.0 (детали, история решения) оставлен ниже как исторический контекст, не как шаг к выполнению.

**Целевая картина (согласована 2026-08-31, вечер; порядок работы уточнён 2026-09-04) — сверху вниз:**
1. `user-note` — одно приложение с одним поведением. Описывается один раз (максимум два: sync/reactive — и только из-за конфликта webmvc/webflux на classpath). Всё остальное — сменная реализация. **Уточнение 2026-09-04**: граница sync/reactive (как и вендорная граница) — чисто техническая, вынужденная сборкой (`WebApplicationType.deduceFromClasspath()`, driver bloat), не поведенческая — для потребителя API (и для порт-теста через `WebTestClient`/`TestRestTemplate` на реальном порту, см. п.1 ниже) разницы нет вообще, контракт один. Та же логика, что уже привела к «HTTP-контракт один на все 5 модулей» — распространяется и на смысл самого разделения sync/reactive: оно существует ради компилятора/classpath, не ради разного поведения.
2. Абстракция = порт (`UserNoteInterface`/`…Reactive`) + `application-core`(`-reactive`) (общий `@SpringBootApplication` + тест поведения на порт в `testFixtures`) + convention-плагин `com.example.user-note-application`(`-reactive`) — **возникает по ходу TDD, не выносится заранее** (пересмотрено 2026-09-04, см. выше).
3. Конкретные composition-root различаются **ТОЛЬКО составом classpath**. Каждый `application-X` = «плагин абстракции + плагин адаптера», ни своей Java, ни своего теста — это целевое состояние ПОСЛЕ TDD, не стартовая точка.
4. Ось адаптеров = SQL vs non-SQL. SQL: один адаптер на модель доступа (`jdbc`/`jpa`/`r2dbc`) покрывает все SQL-вендоры, вендор = профиль. Non-SQL (Cassandra/Couchbase/Neo4j/Elasticsearch/DynamoDB/Redis/Mongo/in-memory): каждый вендор — свой адаптер и свой модуль. sync/reactive удваивает каждый, где драйвер позволяет.
5. Тесты — та же архитектура, что код: один тест на порт, параметризован, гоняется против каждой реализации; реализация удовлетворяет тест, тест не зависит от реализации. **Два уровня, не один** (уточнено 2026-09-04, см. п.1 ниже): тест на порт (service+persistence, без HTTP) и E2E-тест через HTTP (тоже кандидат на shared testFixtures — HTTP-контракт не должен зависеть от технологии персистенции). **Почему один и тот же тест на N реализаций — доказательство «разницы нет для конечного пользователя», а не просто совпадение** (обсуждено 2026-09-04): рассмотрена и отклонена альтернатива — differential testing (гонять одну последовательность операций против пары реализаций и сверять вывод байт-в-байт, без явных assert-ов; так тестируют компиляторы, где оракул писать дорого). Отклонено для этого домена: (1) CRUD-сущность — оракул пишется руками легко, не тот случай; (2) differential ловит только **расхождение** между реализациями — общий баг, одинаковый во всех пяти (например в самом контракте порта), differential не найдёт, а explicit-assert найдёт, если он нарушает спецификацию; (3) N-парных сравнений растёт комбинаторно с числом модулей, один прогон общего теста — линейно. Единственная реальная дыра conformance-подхода — ловит только то, что явно assert-ит автор теста (неучтённое расхождение вроде порядка полей JSON/формата таймстампа может остаться незамеченным) — принята как цена, оправданная простотой домена; differential остаётся кандидатом на будущее, если домен усложнится (не сейчас).
6. Это корневая идея проекта, рекурсивно: hexagonal в коде + convention-плагины на уровне системы — и та же связка абстракция/реализация/DIP на слое composition-root.
7. Порядок работы (пересмотрено 2026-09-04): (1) TDD против порта, код по слоям вниз, абстракция появляется по ходу → (2) compose для dev runtime.

1. **Полный TDD против порта — тест написан НА АБСТРАКЦИИ, не на реализации, порт и абстракция выводятся ПО ХОДУ, не заранее** (объединяет прежние п.0+п.1, пересмотрено 2026-09-04) — тест поведения **один**, зависит **только от `UserNoteInterface` / `UserNoteReactiveInterface`** (не от `DataSource`/`MongoTemplate`), параметризован технологией и вендором (`@ParameterizedTest`/`@ActiveProfiles` по профилям), гоняется против каждого из 5 модулей. Конкретный адаптер модуля удовлетворяет контракт абстракции — реализация подставляется под тест, не наоборот. `getDatabaseProductName()`/`buildInfo`-ассерты и любые `abstract`-пробы удаляются — это связанность абстракции с реализацией. **К тестам — те же паттерны проектирования и та же архитектура, что к коду приложения** (принципиальное требование пользователя, не деталь оформления). sociable/classicist-стиль, реальный Spring-контекст, Testcontainers (принцип — «Завершено» → «2026-08-18, архитектура тестирования»). От тестов — реальный доменный/persistence/presentation код (нижние слои, идём вниз).
   - **Два уровня теста, оба на composition-root, оба — кандидаты на shared testFixtures** (обсуждено 2026-09-04): (а) **тест на порт** — `@Autowired UserNoteInterface`, проверяет service+persistence, минуя контроллер; (б) **E2E-тест через HTTP** — отдельный, не то же самое, ловит то, что порт-тест не видит (маппинг JSON, `@Valid`, `NoteExceptionHandler`→`ProblemDetail`, HTTP-статусы). Рекомендация по механике E2E: `@SpringBootTest(webEnvironment = RANDOM_PORT)` + `TestRestTemplate` (webmvc) / `WebTestClient` (webflux) на реальном порту — **не** `@AutoConfigureMockMvc`/`@AutoConfigureWebTestClient`-in-container (тот не гоняет через реальный сокет, тот же компромисс, что и мок, только тоньше — противоречит уже принятому sociable/classicist-принципу). Оба уровня — максимум 2 варианта каждый (sync/reactive), как и порт-тест — не отдельная новая абстракционная задача, тот же `application-core`.
   - **Уточнение того же вечера — E2E не удваивается на sync/reactive, порт-тест удваивается** (пользователь поймал ошибку в исходной рекомендации). Порт-тест — максимум 2 варианта, механически неустранимо: `UserNoteInterface.add(...)` возвращает `UserNote`, `UserNoteReactiveInterface.add(...)` — `Mono<UserNote>`, разные Java-типы, один тестовый метод не годится для обоих без блокирующего/реактивного моста (убивает смысл теста реактивного пути). E2E/HTTP-тест — **один**, не два: при `@SpringBootTest(webEnvironment = RANDOM_PORT)` и `WebTestClient.bindToServer().baseUrl("http://localhost:"+port)` (или `TestRestTemplate`) клиент работает поверх реального сокета через HTTP — ему не важно, blocking или reactive сервер на другом конце, это чистый протокол. Различие sync/reactive у тест-клиента существует только в in-container-режиме (`@AutoConfigureMockMvc`↔`DispatcherServlet`, `@AutoConfigureWebTestClient`↔`WebHandler` напрямую, без сокета) — который уже отвергнут выше в пользу настоящего HTTP. Итог: HTTP-контракт (JSON/статусы) один на все 5 модулей, один E2E-тест-класс их все покрывает.
   - **Форма самого теста — один сценарный метод («flow»), не россыпь независимых assert-ов** (обсуждено 2026-09-04, вопрос пользователя «как протестировать, что у пользователя один user flow вне зависимости от технологии» — про форму теста, не про механику расшаривания, изначально понято неверно). «Один user flow» доказывается не тем, что пять реализаций проходят похожие, но раздельные `@Test`-методы (`addThenFindByIdReturnsSameUserNote()`, `removeThenExistsByIdReturnsFalse()` и т. п. по отдельности) — а тем, что это буквально **один сценарный метод** (условно `userManagesTheirNoteEndToEnd()`), описывающий реальную последовательность действий пользователя (создал → прочитал → изменил → удалил → убедился, что удалено) целиком в одном потоке, лежащий в `testFixtures`, прогоняемый целиком против каждого из 5 модулей. Сам факт «это один метод, не пять» и делает идею «один flow» видимой в коде теста, а не только в механизме его расшаривания (testFixtures byte-identical class — гарантия, что тест не разъедется, но не про форму самого сценария).
   - Механику расшаривания тестов (testFixtures + convention-плагин / test-jar) выбрать по ходу, когда появится реальный контент для расшаривания — не заранее.
2. **Docker Compose** — `compose.yaml`, делается **после** TDD, при заходе на dev runtime (`bootRun`). Механика профиль↔compose (`spring.docker.compose.profiles.active` в `application-<vendor>.properties`, `profiles: [<vendor>]` на сервисах, `spring.docker.compose.enabled=false` в `application-h2.properties`) — «Активная нить» → «2026-08-31, properties и профили ↔ compose/Testcontainers». Тогда же вернутся вендорные `application-<vendor>.properties` — уже с мост-строкой. Пересекается с открытым вопросом «Единый источник истины для версий»

**Исторический контекст (снятый план п.0, оставлен для истории, не для выполнения)**: детальный разбор, зачем и как выносить абстракцию отдельным шагом — «Активная нить» → «2026-08-31 (вечер), разбор» и «…детализация абстракции + SQL/non-SQL». Три артефакта абстракции (порт + `application-core`(`-reactive`) + convention-плагин `com.example.user-note-application`(`-reactive`)) описаны там же — сама форма абстракции не отменена, отменена только идея выносить её ДО TDD.

### Активная нить: переписывание `user-note/application/` (хронология, новое сверху)

- **2026-08-31, properties срезаны до `problemdetails`; профили ↔ compose/Testcontainers разобраны; следующий шаг — полный TDD** — консультация + правка:
  - **Механика (консультация)**: `DataSource`/`ConnectionFactory`/`MongoClient` в Boot 4 строятся из бина-посредника `Jdbc/R2dbc/MongoConnectionDetails`. Три источника по приоритету: (1) `@ServiceConnection`-контейнер (только тест), (2) `spring-boot-docker-compose` (dev/`bootRun`, если подключён), (3) `Properties*ConnectionDetails` из `spring.datasource.*`/`spring.r2dbc.*`/`spring.mongodb.*` (fallback, `@ConditionalOnMissingBean`). В каждом рантайме активен ровно один; `spring.docker.compose.skip.in-tests=true` по умолчанию → в тестах (2) молчит. Итог: connection-свойства в тестах игнорируются целиком (порт случайный, креды — дефолты контейнера); `application-<vendor>.properties` «живут» только в автономном `bootRun` без compose. embedded H2/R2DBC-H2 fallback держится на том, что `spring.*.url` нигде не задан.
  - **Профиль ↔ compose**: `spring-boot-docker-compose` НЕ читает `spring.profiles.active`. Мост — вручную: `spring.docker.compose.profiles.active=<vendor>` в `application-<vendor>.properties` + `profiles: [<vendor>]` на сервисах в `compose.yaml` + `spring.docker.compose.enabled=false` в `application-h2.properties`. Spring-профиль и compose-профиль — разные механизмы. Тайминг ок (`Environment` готов до `ApplicationPreparedEvent`). Тесты не задеты — `@Profile`-бины контейнеров только в `UserNoteTestConfiguration` (`@TestConfiguration`).
  - **Правка**: удалено 10 profile-specific properties (`application-{h2,mysql,postgresql}.properties` в jdbc/jpa, `application-{mysql,postgresql}.properties` в r2dbc, `application-mongodb.properties` в двух Mongo). В каждом из 5 модулей остался только `application.properties` с `spring.{mvc,webflux}.problemdetails.enabled=true` (пользователь явно оставил — осознанный конфиг presentation-слоя). `h2`-ветки тестов теперь без единого property (embedded-fallback). `clean build` зелёный, все Testcontainers-ветки прошли.
  - **`docs/file-catalog.md`**: секция `### user-note/` переписана 256→62 строк под 5 скелетных модулей; удалённый гексагональный слой свёрнут в блок `[REMOVED] 2026-08-29`; 10 удалённых properties — строки `[REMOVED] 2026-08-31`. Секции `### user/`/`### note/` в каталоге всё ещё устаревшие (сервисы удалены 2026-08-29) — синхронизация отложена.
  - **Обновлено вечером — следующий шаг пересмотрен**: см. запись «2026-08-31 (вечер), разбор: вывести абстракцию application-модулей» ниже. Модули НЕ сворачиваем; выводим абстракцию, которую все 5 реализуют. Всё закоммичено.
- **2026-08-31 (вечер), разбор: вывести правильную абстракцию application-модулей (модули НЕ сворачивать)** — длинный диалог, пользователь настойчиво (с резкой реакцией на увиливание ассистента в сторону вариантов/полумер и на **ошибочный вывод ассистента «свернуть модули»**) провёл мысль до конца:
  - **Тезис пользователя**: каждый из 5 `user-note/application-*` модулей делает ОДНО И ТО ЖЕ, включая КАЖДЫЙ тест в них. Различия — из-за технологий И ТОЛЬКО. Это ошибка проектирования: **нельзя зависеть от реализации**. Задача и суть каждого composition-root — одна.
  - **Вывод (принят, с поправкой)**: 5 модулей — это **разные реализации одного и того же**, и разделение **осознанное/правильное**, вынуждено classpath (ровно один persistence-адаптер + драйвер на модуль; servlet и reactive не сосуществуют — `WebApplicationType.deduceFromClasspath()` → SERVLET при webmvc+webflux вместе). **Модули НЕ сворачивать.** Задача — вынести **одну абстракцию (максимум две: sync + reactive)**: `@SpringBootApplication`, сценарий теста, тестовую инфраструктуру (`UserNoteTestConfiguration` и пр.) — в общее место (shared-модуль / test-fixtures + convention-плагин / test-jar); каждый из 5 модулей её **реализует/переиспользует**, а не переписывает. Модуль поставляет только своё — свой адаптер на classpath + convention-плагины. Дублированной Java в идеале ноль.
  - **Тест — один, написан НА АБСТРАКЦИИ, не на реализации**: зависит только от порта (`UserNoteInterface`/`UserNoteReactiveInterface`), параметризован технологией+вендором, гоняется против каждого из 5 модулей. Конкретный адаптер модуля удовлетворяет контракт абстракции — реализация подставляется под тест, не наоборот. `getDatabaseProductName()`/`buildInfo`-ассерт и предложенный ассистентом `abstract String connectedStore()` — обе формы той же ошибки: реализации (JDBC-способ/R2DBC-способ/Mongo-способ) вылепливают форму общего кода. Абстракцию определяет задача, реализации втыкаются в неё.
  - **Архитектура — не только в коде приложения, но и в тестах**: к тестам применяются те же паттерны проектирования и та же архитектура, что к коду. Тест — не зависит от реализации; одна (максимум две) тестовая абстракция, реализации подставляются. Принципиальное требование пользователя, не деталь оформления.
  - **Отклонённые ассистентом полумеры** (пользователь их отверг как ту же ошибку в миниатюре): «5 тонких root + shared `common`», «3 abstract тест-класса», «однострочник-подкласс ×5», «инжектить `StoreProbe` как порт», **и ошибочное «свернуть 5 модулей в 1/2»**. Тонкий клон — всё равно клон; но и слияние модулей — не решение (classpath).
  - **Порядок дальше — сверху вниз**: (0) вывести абстракцию composition-root (модули остаются 5); (1) TDD против порта; (2) ниже по слоям; (3) compose. См. «Запланировано на следующие сессии».
  - **Мета**: ассистент несколько раз подряд уходил в варианты/оговорки/«подождём TDD», а затем сделал прямо противоположный задаче вывод («свернуть модули») — резкая эскалация пользователя («почему ты вечно смотришь куда-то в сторону», «думай лучше», «ЧТО ТЕБЕ ЗДЕСЬ НЕ ПОНЯТНО», «да что с тобой такое»). Урок: когда пользователь ведёт чёткую мысль — идти С НИМ до конца по его линии, не подменять её своим (тем более обратным) выводом. См. [[feedback_follow_user_reasoning_to_conclusion]].
- **2026-08-31 (вечер), детализация абстракции application-модуля + принцип SQL/non-SQL** — продолжение разбора выше:
  - **Абстрактный application-модуль = три артефакта** (детали — «Запланировано на следующие сессии» п. 0): порт `UserNoteInterface`/`…Reactive` + Java-модуль `application-core`(`-reactive`) (общий `@SpringBootApplication` + testFixtures с тестом поведения на порт) + convention-плагин `com.example.user-note-application`(`-reactive`). Конкретный composition-root = `build.gradle.kts` из «плагин абстракции + плагин(ы) адаптера», своей Java ~ноль. **Application-модули различаются ТОЛЬКО составом classpath** (адаптер + драйвер), не кодом/тестами/конфигом.
  - **Организующий принцип — SQL против non-SQL** (уточнение пользователя, принято):
    - **SQL-вендоры делят один адаптер на модель доступа.** `data-jdbc`/`data-jpa`/`data-r2dbc` — каждый покрывает ВСЕ SQL-вендоры (H2, MySQL, PostgreSQL, MariaDB, Oracle, MSSQL, CockroachDB…) сменой драйвер+диалект+`schema-{vendor}.sql`. Вендор внутри — профиль/параметр, НЕ модуль и НЕ новый адаптер. Три модуля здесь (`jdbc`/`jpa`/`r2dbc`) — это три *модели доступа*, не «три SQL»; вендорная ось в каждой ортогональна.
    - **Non-SQL-вендоры — каждый свой адаптер → свой application-модуль**, потому что `CassandraRepository`/`CouchbaseRepository`/`ElasticsearchRepository`/Neo4j/Redis(RedisHash)/DynamoDB(Enhanced SDK, Spring Data не покрывает)/in-memory не делят ни строки кода. `application-mongodb` уже ровно такой случай.
    - **sync/reactive** режет каждый non-SQL-вендор ещё на два (адаптер под `UserNoteInterface` и под `UserNoteReactiveInterface`), где драйвер это позволяет — как Mongo сейчас.
    - Все — SQL и non-SQL — реализации одного абстрактного application-модуля, прогоняют один и тот же тест поведения на уровне порта.
  - **Примеры будущих адаптеров/модулей**: sync — `data-jooq`/`data-jdbi`/`data-ebean` (иной API поверх SQL), `data-cassandra`, `data-couchbase`, `data-neo4j`, `data-elasticsearch`, `data-dynamodb`, `data-redis`, `data-inmemory` (Map-адаптер: эталон контракта + быстрый тест-дубль); reactive — `-cassandra-reactive`, `-couchbase-reactive`, `-neo4j-reactive`, `-elasticsearch-reactive`, `-redis-reactive`, `-inmemory-reactive`, `data-r2dbc` на MariaDB/MSSQL/Oracle. Driving-ось (другой «flavor» абстрактного application, не vendor-адаптер): `presentation/graphql`/`-grpc`/`-rsocket`.
  - **Наблюдение пользователя**: даже на этом слое (composition-root / модули / сборка) вырисовывается та же архитектура, что в любом программном проекте — одна абстракция + много реализаций, инверсия зависимостей, тест на абстракцию. Это «корневая идея» проекта (два уровня изоляции — hexagonal в коде сервиса + convention-плагины на уровне системы), проявляющаяся рекурсивно на уровне composition-root.
- **2026-08-31, Checkstyle `SpringLambdaCheck` приведён к SonarQube `java:S1611`** — у разработчика в IDEA включён SonarQube (SonarLint), правило `java:S1611` требует убирать скобки у одиночного параметра лямбды с выводимым типом (`connection ->`, не `(connection) ->`) — прямой конфликт с дефолтом бандла Spring (`SpringLambdaCheck` с `singleArgumentParentheses=true` требует скобки; проявился на `application-r2dbc/UserNoteApplicationTests` после правки пользователя `(connection) -> ` → `connection ->`, `checkstyleTest` покраснел). Тот же класс конфликта, что `SpringImportOrderCheck`/`InterfaceIsType`/`WhitespaceAroundCheck` — бандл Spring vs другой инструмент. Решение — не исключение чека целиком, а флип его свойства (проверено декомпиляцией `spring-javaformat-checkstyle-0.0.48.jar`: `SpringLambdaCheck` несёт `boolean singleArgumentParentheses`, дефолт `true` в конструкторе; при `false` чек логирует `lambda.unnecessaryParen` на `(x) ->`, т. е. начинает требовать ровно то же, что S1611; под-проверка `lambda.unnecessaryBlock` сохраняется в обоих режимах). В `gradle/checkstyle/checkstyle.xml`: `io.spring.javaformat.checkstyle.check.SpringLambdaCheck` добавлен в `excludes` `SpringChecks` и переобъявлен явно в `TreeWalker` c `<property name="singleArgumentParentheses" value="false"/>` — по образцу `WhitespaceAroundCheck` (2026-08-29). Грепом по всему репозиторию — единственная затронутая лямбда была та самая r2dbc-я, уже без скобок. `clean build --continue` зелёный на всех 5 модулях. SonarQube в саму сборку/CI по-прежнему НЕ подключён (только SonarLint у разработчика в IDE) — это подгонка Checkstyle под конвенцию S1611, не принятие SonarQube как гейта.
- **2026-08-31, незакоммиченные правки пользователя в 5 `UserNoteApplicationDefaultTest` — переименование abstract-баз «по инжектируемому клиенту/абстракции технологии»** (поверх коммита `0625282`): `application-jdbc`/`application-jpa` — `DatabaseProductNameTests` → `DataSourceTests` (обе технологии сидят на `DataSource`, общее имя = общая абстракция, та же пара делит один вариант при будущем выносе в fixtures); `application-r2dbc` — → `DatabaseClientTests` (реактивный реляционный доступ через `DatabaseClient`, не `DataSource`) + убраны скобки у лямбды (см. запись про S1611 выше); `application-mongodb` — `ServerInfoTests` → `MongoTemplateTests`; `application-mongodb-reactive` — `ServerInfoTests` → `ReactiveMongoTemplateTests`. Итого 4 разных имени (не 2) — корректно, т. к. blocking и reactive базы генуинно разные. Пп. 3/9 записи «2026-08-29» ниже и абзац «Статус по сервисам» приведены к этим четырём именам.
- **2026-08-31, `gradle.properties` — пользователь убрал `org.gradle.configureondemand=true` и `org.gradle.configuration-cache.parallel=true`** (+ добавлен финальный перевод строки). Осталось: `parallel`/`caching`/`configuration-cache`/`configuration-cache.problems=fail`. `configureondemand` при многомодульной сборке мог оставлять часть проектов/sourceSet'ов ненастроенными до полного sync (симптом «пропали test sources» в IDEA) — снятие флага устраняет. `clean build` зелёный после снятия обоих.
- **2026-08-29 (вечер), сводка длинной сессии — режим работы и мета-наблюдения**:
  - **Режим**: эта фаза — не «ассистент только консультирует». Пользователь вёл серией мелких инкрементальных команд на правку (`добавь`/`отдели`/`раздели`/`причеши`/`закончи`/`коммит`), ассистент их выполнял. То есть на данном этапе (скелетные composition-root модули `user-note/`) правки Java/Gradle/properties и git-операции ассистент делает по прямому запросу. Ранняя оговорка сессии: пользователь резко отреагировал на правку файла во время его же запроса «посмотри, всё нормально?» (ревью ≠ разрешение править) — правило «ревью значит ревью» осталось в силе, изменился только общий контекст (пошли явные команды на правку).
  - **Мета-ошибка ассистента (дважды подряд, резкая реакция)**: уверенно назвал `spring.data.mongodb.host` актуальным, потом «исправил» на `spring.data.mongodb.uri` — оба deprecated (см. отдельную запись ниже). Урок закреплён: имя Spring-свойства — грепом по метаданным установленного jar, не по памяти.
  - **Наблюдение (переиспользуемый Spring-факт)**: R2DBC `io.r2dbc.spi.ConnectionMetadata.getDatabaseProductName()` **НЕ нормализован** как JDBC `DatabaseMetaData.getDatabaseProductName()` — MySQL-драйвер отдаёт сырую строку сервера `"MySQL Community Server - GPL"` (H2/PostgreSQL совпали с `"H2"`/`"PostgreSQL"`). Поэтому в r2dbc-тесте ассерт `.contains(...)`, а не `.isEqualTo(...)`.
  - **Наблюдение**: `org.springframework.r2dbc.core.DatabaseClient` — автоконфигурируемый бин (конструктор `DataR2dbcAutoConfiguration` требует его параметром), `.inConnection(fn)` сам управляет lifecycle connection — чище, чем ручной `Mono.usingWhen(cf.create(), ..., Connection::close)`.
  - **Наблюдение**: Checkstyle `SpringLambdaCheck` по умолчанию (`singleArgumentParentheses=true`) требует скобки у одиночного параметра лямбды (`(x) ->`, не `x ->`) — **отменено 2026-08-31**, см. запись «2026-08-31» ниже (правило приведено к SonarQube `java:S1611`: теперь запрещены скобки, требуется `x ->`).
  - Коммиты сессии: `046bb56` (реструктуризация + удаление note/user), `8947a05` (отделение application-mongodb), `d89f329` (rename application-reactive→application-r2dbc), `bdb3e17` (5 симметричных модулей + доводка r2dbc/mongo-reactive + причёсывание).
- **2026-08-29 (вечер), реструктуризация `user-note/` → 5 плоских модулей-скелетов + весь гексагональный слой удалён** (обгоняет 16-подзадачную разбивку выше). `git rm` намеренно: `note/`+`user/` целиком (396 файлов), в `user-note/` — `domain/`+`persistence/data-*`+`presentation/` + старые `application/*`. Итог — 5 плоских composition-root скелетов без `dependencies{}`, пакет `com.example.usernote`, голый `@SpringBootApplication`, все зелёные (`clean build`, Docker UP). Серией правок (коммиты `046bb56`→`8947a05`→`d89f329`→`bdb3e17`): Mongo отделён из sync-модуля в свой; `application`→`application-jdbc` + копия `application-jpa` (различие — один плагин `data-jdbc`/`data-jpa`; механизм `@Profile("jdbc"/"jpa")` из «2026-08-20» этим отменён); `application-reactive`→`application-r2dbc`, доведён под R2DBC; выделен `application-mongodb-reactive`. **Причёсывание под единую форму** («чтобы проще выделять общее»): `UserNoteApplication.java`/`package-info.java`/`UserNoteTestApplication.java` — байт-в-байт во всех 5; `UserNoteApplicationDefaultTest` — единая форма (outer `@SpringBootTest+@Import` → abstract база по клиенту → `@Nested @ActiveProfiles` ветки); 3 реляционных `UserNoteTestConfiguration` байт-в-байт, 2 Mongo байт-в-байт. Текущая раскладка модулей/тестов/плагинов — «Статус по сервисам». Technology-mandated различия (не убираются): `webmvc`↔`webflux`; клиент `DataSource`/`DatabaseClient`/`MongoTemplate`/`ReactiveMongoTemplate`; ассерт `isEqualTo` (JDBC норм.) ↔ `contains` (R2DBC — нет) ↔ `containsKey("version")` (Mongo — нет product name).
- **2026-08-29, `spring.data.mongodb.*` → `spring.mongodb.*` (весь префикс, deprecation level=error, since Boot 4.0.0)** — конкретная ошибка ассистента: сначала записал `spring.data.mongodb.host=...`, потом «исправил» на `spring.data.mongodb.uri=...` — оба deprecated. Проверено по `spring-boot-mongodb-4.1.0.jar` → `META-INF/additional-spring-configuration-metadata.json`: в Boot 4.0 connection-слой Mongo вынесен в отдельный модуль `spring-boot-mongodb` (`MongoConnectionDetails`), поверх строит `MongoTemplate` модуль `spring-boot-data-mongodb`. Под новым префиксом `spring.mongodb.*` не deprecated ни `uri` (default `mongodb://localhost/test`), ни дискретные `host`/`port`/`database`/`username`/`password`/`authentication-database`/... — обе формы валидны. Правило: имя любого Spring-свойства сверять грепом по `spring-configuration-metadata.json`/`additional-spring-configuration-metadata.json` внутри реально установленного jar, не по памяти. **Механизм разрешения адреса Mongo** (аналогично `JdbcConnectionDetails`, см. запись «2026-08-29, механизм `@ServiceConnection`»): бин `MongoConnectionDetails`; `@ServiceConnection`-контейнер и `spring-boot-docker-compose` дают свой бин, перекрывающий `PropertiesMongoConnectionDetails` (из `spring.mongodb.*`). То есть `application-mongodb.properties` читается только на голом `bootRun --spring.profiles.active=mongodb`; в тестах его перекрывает `@ServiceConnection` от `MongoDBContainer` (реальный порт, `MongoDBContainer` по умолчанию без auth).
- **2026-08-29, пустые тела `{}` в одну строку — Checkstyle разрешил, Spotless теперь схлопывает, IDE-паритет решается отдельным Eclipse-профилем (не `.idea/`)** — цепочка решений одного дня, каждое проверено эмпирически:
  1. Добавлен приватный конструктор в `UserNoteTestApplication` (utility-class паттерн, Effective Java item 4) — вскрыл, что `checkstyle.xml` требовал ровно ОДНУ пустую строку внутри пустого тела (`RegexpMultilineCheck`, формат `\{[ \t]*(\n[ \t]*)?\}`, сообщение по-русски) — несовместимо с обычным `{}`. По прямому запросу пользователя правило **удалено** из `checkstyle.xml`, вместе с ним всплыл второй конфликт — `WhitespaceAroundCheck` (часть `io.spring.javaformat.checkstyle.SpringChecks`) требует пробел вокруг `{`/`}`, тоже запрещая голый `{}`. Решено так же: `WhitespaceAroundCheck` добавлен в `excludes` `SpringChecks` и переобъявлен явно в `TreeWalker` с `allowEmptyConstructors/Methods/Types/Loops/Catches/Lambdas=true` (проверено декомпиляцией `checkstyle-13.10.0.jar` — свойства реальны, дефолт бандла Spring это же свойство вообще не переопределяет, см. `spring-checkstyle.xml` внутри `spring-javaformat-checkstyle-0.0.48.jar`). `checkstyleMain`/`checkstyleTest` по всему репозиторию зелёные после обеих правок
  2. Найдено и задокументировано (аудит по прямому запросу): единственное оставшееся русскоязычное место в коде/конфигурации — `checkstyle.xml` → сообщение другого, всё ещё активного `RegexpMultilineCheck` («Пустая строка перед закрывающей скобкой не допускается», отдельное правило — про пустую строку перед `}` в непустом теле, не трогали). Не переведено — ждёт решения пользователя
  3. **IntelliJ IDEA не подключён к `checkstyle.xml` вообще** — `.idea/checkstyle-idea.xml` (не в git, `.idea` в `.gitignore`) указывает только на два bundled-набора (Sun/Google Checks), `activeLocationIds` пуст — плагин Checkstyle-IDEA в принципе не форматирует код (только инспекции), реформатирование (`Cmd+Opt+L`) идёт через полностью отдельный движок IntelliJ, управляемый Code Style-схемой. `.idea/codeStyles/` не содержал `Project.xml` — использовалась личная глобальная схема разработчика, ни с чем в репозитории не связанная
  4. Локально (только на этой машине, не в git) создан `.idea/codeStyles/Project.xml` (`KEEP_SIMPLE_METHODS_IN_ONE_LINE`/`KEEP_SIMPLE_BLOCKS_IN_ONE_LINE`/`KEEP_SIMPLE_CLASSES_IN_ONE_LINE`/`KEEP_SIMPLE_LAMBDAS_IN_ONE_LINE=true`) + `codeStyleConfig.xml` (`USE_PER_PROJECT_SETTINGS=true`) — эмпирически не сработало предсказуемо: по исходникам `JetBrains/intellij-community` (`JavaSpacePropertyProcessor.shouldHandleAsSimpleMethod`) эти настройки **сохраняют** уже однострочное тело при реформатировании, а не **схлопывают** уже развёрнутое многострочное — семантика «keep», не «force». Пользователь явно отказался коммитить `.idea/` в репозиторий в любом случае — вариант снят как нерешающий задачу «командная разработка, разные IDE»
  5. **Рабочее решение для гарантии на уровне сборки** — `com.example.codequality-spotless.gradle.kts`, новый шаг `replaceRegex("collapseEmptyBraces", "\\{\\s*\\}", "{}")` внутри `java {}` — точечная regex-замена, не полноценный форматтер (сознательно, после отклонённого варианта «взять Spotless `eclipse()`-форматтер целиком» — это переформатировало бы вообще всё: отступы/переносы/выравнивание, тот же класс проблемы, из-за которого раньше убрали `io.spring.javaformat`, см. «Завершено» → «2026-07-13»). Проверено `spotlessApply` по всему репозиторию — diff чистый, тронуты только реальные пустые тела (`contextLoads(){}` в 4 модулях, конструктор в `UserNoteTestApplication`), никакой побочной правки. `compileJava` уже зависит от `spotlessApply` — эффект автоматический при любой сборке, любой IDE, без разницы, какой личный Code Style настроен у разработчика
  6. **Live-паритет в самой IDE (`Cmd+Opt+L` до всякой сборки) — рассмотрен и закрыт как недостижимый в заданных рамках, не реализовывать без пересмотра рамок** — черновик `gradle/eclipse/eclipse-java-formatter.xml` (портируемый Eclipse formatter-профиль: индент 4 пробела, K&R-скобки, все 7 `insert_new_line_in_empty_*` → `do not insert`) был собран, затем удалён по прямому запросу пользователя. Причина закрытия, не просто предпочтение — технический факт об IntelliJ: единственный канал, которым IDE **сама**, без ручного действия разработчика, подхватывает конфигурацию форматирования при открытии проекта — это `.idea/codeStyles/` в директории проекта; `Import Scheme` (в т. ч. из Eclipse XML) всегда ручное, разовое действие, автоматического импорта не существует в принципе. Пользователь исключил `.idea/` из репозитория полностью, включая узкий вариант «закоммитить только `.idea/codeStyles/`, остальное `.idea/` в `.gitignore`» (официально задокументированный JetBrains-паттерн для шаринга именно code style, не общих личных настроек — тоже предложен и отклонён). Требования «автоматически для всех разработчиков» и «`.idea/` не трогаем вообще» взаимоисключающие для IntelliJ конкретно, не разные степени одного решения — этим и закрыт вопрос, а не отсутствием технической возможности с чьей-либо стороны. **Итоговое решение** — только гарантия на уровне сборки (см. п. 5, `replaceRegex` в Spotless): единообразие закоммиченного/собранного кода, независимо от IDE, но не то, что видно live при наборе текста. Пересматривать только если пользователь пересмотрит рамку «`.idea/` не трогаем» — не предлагать заново то же самое без этого
  7. **Альтернативная переформулировка задачи п. 6 (не реализовано, идея на будущее)** — вместо синхронизации настроек двух разных движков форматирования (IntelliJ Code Style ↔ Eclipse/Spotless), запускать во время разработки тот же самый Spotless, что уже используется на сборке — тогда рассинхронизации форматтеров не может быть в принципе, инструмент один. Варианты, не проверены: (а) `./gradlew spotlessApply --continuous` в терминале — следит за файлами, кросс-IDE (просто процесс, не зависит от редактора), ручной разовый запуск на сессию разработки; (б) IntelliJ File Watchers plugin — автозапуск на каждое сохранение, специфично для IntelliJ, конфигурация плагина по умолчанию может лечь в `.idea/watcherTasks.xml` — как чисто личный, не шаренный через git инструмент не нарушает «не трогаем `.idea/`», но не решает задачу для всей команды сразу; (в) git pre-commit hook — не поднимался, отдельная тема, не тот же вопрос, что «до сборки в момент разработки»
- **2026-08-29, механизм `@ServiceConnection`/`JdbcConnectionDetails`/старта контейнера — разобран по байткоду (консультация, ничего не менял в коде)** — три вопроса пользователя по `UserNoteTestConfiguration`, ответы проверены декомпиляцией реально установленных jar (`spring-boot-testcontainers-4.1.0.jar`, `spring-boot-jdbc-4.1.0.jar`), не только `docs.spring.io`:
  1. **Порядок старта контейнерного `@Bean`** — `docs.spring.io` → «Lifecycle of Managed Containers» документирует гарантию «container beans are created and started before all other beans» дословно, но не называет механизм. Байткодом найдены классы `org.springframework.boot.testcontainers.lifecycle.{TestcontainersLifecycleApplicationContextInitializer, TestcontainersLifecycleBeanFactoryPostProcessor, TestcontainersLifecycleBeanPostProcessor}` — реализация именно через `BeanFactoryPostProcessor`+`DestructionAwareBeanPostProcessor`, не через `@Order`/`@DependsOn`
  2. **`JdbcConnectionDetails` — обязательная прослойка в Boot 4.1, не опция `@ServiceConnection`** — байткод `DataSourceConfiguration$Hikari.dataSource(DataSourceProperties, JdbcConnectionDetails, Environment)` показывает, что метод создания `HikariDataSource`-бина принимает `JdbcConnectionDetails` как обязательный параметр всегда, вне зависимости от наличия `@ServiceConnection`. В том же jar (`spring-boot-jdbc`) лежит `PropertiesJdbcConnectionDetails` — fallback-реализация, оборачивающая `spring.datasource.*` из properties. Значит `DataSource`-бин **создаётся в обоих случаях**; `@ServiceConnection` не включает/выключает его создание, а лишь подменяет, какой именно `JdbcConnectionDetails`-бин победит (`@ConditionalOnMissingBean`-подобный приоритет: bean от `@ServiceConnection` — с реальным портом контейнера, иначе — `PropertiesJdbcConnectionDetails` со статичным портом из properties)
  3. **Практическое следствие, если убрать `@ServiceConnection`** — Testcontainers всегда мапит порт контейнера на случайный свободный порт хоста (не совпадает с портом в properties, например `3306`/`5432`). Без `@ServiceConnection`/`@DynamicPropertySource` контекст всё равно поднимается (создание `HikariDataSource`-объекта не требует соединения), но `contextLoads()` падает именно на `dataSource.getConnection()` — на выписанном в properties порту никто не слушает. Худший случай — на этом порту случайно слушает настоящий сервер (например, локальный dev-инстанс MySQL у разработчика) — тест тогда молча подключается не к тому, что нужно, теряя изоляцию. Это общая проблема любого сочетания Testcontainers+Spring Boot, не специфика этого проекта — `@DynamicPropertySource` (более старый способ, предшествующий `@ServiceConnection`) решает её вручную тем же путём
- **2026-08-29, теги Docker-образов на `:latest`, Gradle wrapper 9.7.0→9.7.1 (потерян `distributionSha256Sum`)** — `UserNoteTestConfiguration` переведён с pinned `mysql:9`/`postgres:18` на `new MySQLContainer(DockerImageName.parse("mysql:latest"))`/аналогично PostgreSQL — изменение сделано пользователем в IDE, не ассистентом. Тем самым конкретный пример «версия образа расходится между окружениями» из записи «2026-08-20, версия Docker-образа» ниже сейчас не воспроизводится (тег вообще не пинуется), но сам вопрос «Единый источник истины для версий» (см. «Открытые решения») этим не закрыт — `:latest` меняется во времени без предупреждения, обычно противопоказан именно для воспроизводимости тестов, скорее переносит проблему, чем решает. Параллельно `gradle-wrapper.properties` обновлён 9.7.0→9.7.1 (похоже, автоматическим обновлением wrapper через IDE) и **потерял строку `distributionSha256Sum`** — ту самую защиту дистрибутива Gradle от подмены, что была осознанно добавлена 2026-08-15 («золотой стандарт»); вероятно, побочный эффект инструмента обновления wrapper, вызванного без флага `--gradle-distribution-sha256-sum`. Не восстановлено, ждёт решения пользователя — восстановление требует знать корректную sha256-сумму дистрибутива 9.7.1 (не выдумывать, сверять с `https://gradle.org/release-checksums/` или `gradle wrapper --gradle-version 9.7.1 --gradle-distribution-sha256-sum <...>`)
- **2026-08-21, фактчек `docs.spring.io` → `reference/testing/testcontainers.html` — уточняет запись «2026-08-20, версия Docker-образа»** — прочитана страница целиком (не web-поиском, дословно): 7 разделов — Using Spring Beans / Using the JUnit Extension / Importing Container Configuration Interfaces / Lifecycle of Managed Containers / Service Connections / SSL with Service Connections / Dynamic Properties. Это отдельная страница от `reference/features/dev-services.html` (откуда взят паттерн `@Bean @ServiceConnection`+`TestApplication`+`testAndDevelopmentOnly`+`bootTestRun` в записи «2026-08-20, версия Docker-образа» ниже) — та про запуск приложения на dev-time, эта — про тестовый уровень; обе части одной документации, не противоречат друг другу. **Новый факт, меняющий вес уже открытого вопроса**: раздел «Lifecycle of Managed Containers» **явно** называет управление контейнером через Spring-бин (`@TestConfiguration`+`@Bean`+`@ServiceConnection`) предпочтительным перед JUnit-расширением (`@Testcontainers`+`@Container`) **именно для Spring Boot тестов** — причина прямо документирована: JUnit-managed контейнер может остановиться раньше, чем закроется `ApplicationContext`, что даёт ошибки соединения при завершении контекста. Это прямо касается нынешней реализации `JDBC` (см. «2026-08-20, `@Nested`+`@ActiveProfiles`-тесты реализованы» — `@Testcontainers`+`@Container` static-поле внутри `@Nested`) — по документации это не «альтернативный», а явно менее предпочтительный вариант для данного случая, не только для сценария dev-time запуска. Побочные факты того же раздела `Service Connections`: один `@ServiceConnection`-контейнер JDBC-семейства (`PostgreSQLContainer`/`MySQLContainer`/`MariaDBContainer`/`MSSQLServerContainer`/`ClickHouseContainer`/Oracle) одновременно создаёт и `JdbcConnectionDetails`, и `R2dbcConnectionDetails` — не проверялось, применимо ли к текущему разделению JDBC/R2DBC-модулей. `@ImportTestcontainers` (интерфейс со статическими `@Container`-полями, импортируется в `@TestConfiguration`) — задокументированный официальный механизм расшаривания контейнеров между тестовыми классами/модулями, не рассмотрен как кандидат в открытом вопросе «дублирование сценария» (см. «Завершено» → «2026-08-18, архитектура тестирования» — там как кандидат назван только test-fixtures).
  **Апдейт того же дня — попытка `@ImportTestcontainers` реализована и эмпирически провалилась, вопрос «версия Docker-образа» ниже закрыт другим решением**: попытка (интерфейс `UserNoteContainers` со статическими `@Container`-полями `MySQLContainer`/`PostgreSQLContainer`, импортированный через `@ImportTestcontainers` в `UserNoteTestConfiguration`) наткнулась на 3 находки подряд. (1) Checkstyle `InterfaceIsType` (из `SpringChecks`, не исключён явно) валит именно такой интерфейс — «interfaces should describe a type and hence have methods»: прямой конфликт официального паттерна с гейтом проекта, тот же класс конфликта, что раньше был у `SpringImportOrderCheck`. (2) Эмпирически (не только по доке) подтверждено: `@Import(UserNoteTestConfigutation.class)` висел на внешнем `UserNoteApplicationDefaultTest`, `@Nested`-классы наследуют его безусловно — оба контейнера (`MySQL`+`PostgreSQL`) стартовали и регистрировали `@ServiceConnection`-бины во ВСЕХ трёх `@Nested`-контекстах разом (`H2`/`MySQL`/`PostgreSQL`), включая `H2`; какой из двух конкурирующих `JdbcConnectionDetails`-бинов побеждал — не специфицировано, эмпирически стабильно MySQL (`H2.contextLoads()` и `PostgreSQL.contextLoads()` оба падали с `expected: ... but was: "MySQL"`). Интерфейсные поля `@ImportTestcontainers` не имеют способа стать profile-aware. (3) Попытка повесить `@Profile("mysql")` прямо на поле интерфейса не компилируется вообще — `@Profile` неприменим к `FIELD` (`@Target` аннотации — `TYPE`/`METHOD`), не рантайм-баг, ошибка компиляции. **Решение, реализованное и зелёное на `clean build`**: `UserNoteContainers.java` удалён, `UserNoteTestConfiguration` — `@TestConfiguration`+2 `@Bean`-метода (не поля), каждый с `@Profile("mysql"/"postgresql")`+`@ServiceConnection` — `@Profile` на `@Bean`-методе применим (в отличие от поля), Spring создаёt только бин активного профиля, `@ServiceConnection` по-прежнему переопределяет `DataSource` на реальный порт контейнера. Мёртвые `@Autowired MySQLContainer`/`PostgreSQLContainer`-поля убраны из `UserNoteApplicationDefaultTest` (Error Prone `UnusedVariable`, не были нужны). Закрывает «Открытые решения»/пункт «2026-08-20, версия Docker-образа для Testcontainers vs реальный запуск» ниже по хронологии — паттерн `@Bean`+`@Profile`+`@ServiceConnection` реализован, `@Container`+static-поле внутри `@Nested` (там же) — снят как рабочий, но официально менее предпочтительный вариант
- **2026-08-21, дедуп `contextLoads()`-ассерта по вендорам на `JDBC` (Template Method) + консультация про автозапуск Docker/CI** — три вендорных `@Nested`-класса `H2`/`MySQL`/`PostgreSQL` в `UserNoteApplicationDefaultTest` (см. запись «2026-08-20, `@Nested`+`@ActiveProfiles`-тесты реализованы» ниже) дублировали поле `@Autowired DataSource`+сам ассерт `getDatabaseProductName()`, различаясь только ожидаемой строкой. Вынесено в `abstract static class DatabaseProductNameTests` (Template Method — `expectedProductName()` абстрактный, `contextLoads()` в базовом классе), каждый `@Nested` теперь только `extends` + переопределяет `expectedProductName()`. `checkstyleTest`/`spotlessApply`/`compileTestJava` зелёные; `H2` прогнан вживую (прошёл); `MySQL`/`PostgreSQL` не прогнаны в этой сессии — Docker Desktop был не запущен. **Побочная находка** — во время сессии в истории появился независимый коммит `5233efb` от ДРУГОЙ Claude-сессии (`session_017RF8UpJXDmLwP4UZJYL7rD`), сделавший тот же по сути рефакторинг параллельно и заодно сжавший `CLAUDE.md` 167k→93k символов — commit этой сессии (`967aad3`) лёг поверх него и оказался косметическим дублем (переставил уже существующий класс). Урок: при длинной сессии с параллельной работой пользователя/другой сессии над теми же файлами — перед `git commit` проверять `git log` на неожиданный intervening-коммит, не полагаться на то, что HEAD не сдвинулся с начала разговора.
  Далее — консультация (ничего не реализовано, build-логика не тронута): автозапуск Docker Desktop при отсутствии запущенного демона обсуждён на 3 уровнях. (1) **Системный** — у Docker Desktop есть штатный флаг `AutoStart` (`~/Library/Group Containers/group.com.docker/settings-store.json`, он же GUI-тумблер «Start Docker Desktop when you sign in») — сейчас `false`, не менялся. (2) **Из сборки** — ни Testcontainers, ни Gradle, ни Spring Boot демон сам не поднимают, только проверяют, отвечает ли уже работающий; технически возможно кастомным Gradle-таском (`open -a Docker` + поллинг `docker info`, macOS-специфично, нужна деградация для Linux/Windows) — не реализовано, ждёт «делай» по правилу «без самоуправства в build-логике». (3) **Как это решено в CI/CD** — не автозапуском вовсе, а гарантией «уже поднято» на уровне окружения ДО старта job: GitHub Actions `ubuntu-latest` (уже используется в `.github/workflows/gradle.yml` этого репозитория) — Docker Engine предустановлен, `dockerd` уже работает как системный сервис VM раннера, сборка выполняется прямо на VM (не в контейнере), менять workflow не нужно. Jenkins (в проекте не используется, справочно) — 2 случая: агент = bare VM/bare-metal → тот же принцип, Docker как systemd-сервис хоста; сборка выполняется внутри контейнера → официальный паттерн Testcontainers **«Docker wormhole»/sibling containers** (`java.testcontainers.org` → `dind_patterns`) — монтировать хостовый `/var/run/docker.sock` в контейнер сборки И монтировать каталог исходников по тому же самому пути внутри контейнера (иначе разъедутся volume-мониты для спавнимых Testcontainers-контейнеров); Docker-in-Docker (вложенный демон в privileged-контейнере) официально не рекомендован — «instrument of last resort». Локальный аналог гарантии CI — системная настройка автозапуска (пункт 1), не билд-скрипт.
  **Отдельно, в тот же день — запуск Docker без Docker Desktop** (консультация, ничего не установлено/не реализовано): `dockerd` линуксовый, нативно на macOS не существует ни у одного вендора — Docker Desktop не более чем GUI-приложение, которое в фоне поднимает Linux VM + демон; сама VM нужна независимо от того, открыт ли GUI (можно работать чисто через `docker`-CLI из терминала, ни разу не открыв окно Desktop — фоновый процесс всё равно обязателен). «Стандартного» (от Docker Inc., без VM-обёртки) пути на macOS не существует в принципе — архитектурное ограничение, не пробел в конкретном инструменте. Альтернативы (тоже VM-обёртки, но сторонние, без GUI-приложения) — **Colima** (`brew install colima && colima start`, ближе всего к «просто демон в фоне», без лицензионных ограничений Docker Desktop для крупных компаний), Rancher Desktop, OrbStack (платный после триала для не-личного использования), Podman (rootless, другой демон, не всегда 100% drop-in для Testcontainers). `docker`-CLI/Testcontainers/Gradle работают с любой из них без изменений в build-логике репозитория.
- **2026-08-20, версия Docker-образа для Testcontainers vs реальный запуск** — вопрос: теги `mysql:9`/`postgres:18` в `@Container`-полях должны совпадать с тем, что используется при реальном запуске. Факт: `compose.yaml` для `JDBC` не существует вовсе — дублирования пока физически нет. Найден официальный путь (`docs.spring.io` → «Using Testcontainers at Development Time») — контейнеры как `@Bean @ServiceConnection` в `@TestConfiguration` + отдельный `UserNoteTestApplication` (`SpringApplication.from(UserNoteApplication::main).with(...).run(args)`) + `testAndDevelopmentOnly` вместо `testImplementation` → `./gradlew bootTestRun` (dev) и `test` делят одну конфигурацию, `compose.yaml` становится не нужен вовсе. Под 3-профильную специфику модуля — оба контейнерных бина в одной `@TestConfiguration`, каждый под своим `@Profile("mysql"/"postgresql")`. **Не реализовано** — требует переписать нынешний `@Container`-подход (`Mysql`/`Postgresql` nested-классы) на этот паттерн, ждёт решения пользователя. **Апдейт 2026-08-21** — узкий технический вопрос этой записи закрыт (см. «2026-08-21, `@ImportTestcontainers`... провалилась» выше по хронологии); более широкий вопрос «единый источник истины версии образа во всех окружениях, не только dev/test» вырос в отдельный пункт «Открытые решения» → «Единый источник истины для версий (Docker-образы, Java)»
- **2026-08-20, explicit import groups реализован** — `SpringImportOrderCheck` заменён (только он, не вся `SpringChecks`) на голый `ImportOrderCheck` с явным `groups="java,javax,com.example,jakarta.persistence,org.assertj,org.jspecify,org.junit,org.testcontainers,reactor.core,*,org.springframework"` в `gradle/checkstyle/checkstyle.xml` + тот же список в `com.example.codequality-spotless`'s `importOrder(...)` — пустая строка между КАЖДОЙ группой (было — неделимая `*`-группа у `SpringImportOrderCheck`, подтверждено декомпиляцией: конструктор жёстко зашивает 4 группы). Список составлен грепом реально использованных 2-сегментных пакетов в `note`/`user`/`user-note`. `H2`-`@Nested`-класс дополнен тем же ассертом на `getDatabaseProductName()` (`"H2"`), что уже был у `MySQL`/`PostgreSQL` (раньше — голый `contextLoads()`). `clean check` зелёный по всему репозиторию (`note`/`user` вне сборки — вернутся под эту же схему при возврате в `settings.gradle.kts`). **Открытый вопрос на будущее** — explicit-список не масштабируется («не хочу вручную перечислять группы» по мере роста WIP): ни Spotless, ни Checkstyle не умеют «граница по паттерну», только явный конечный список `groups`. Два пути, не реализовано: (1) кастомный Checkstyle `AbstractCheck`/Spotless custom formatter step, сравнивающий top-level пакет соседних импортов без списка вообще; (2) Gradle-таск, сканирующий `.java`-файлы и **генерирующий** список `groups`/`importOrder(...)` (проще варианта 1 — не пишет чек с нуля), подключаемый через `configProperties`/`${...}`-подстановку в XML (тот же механизм, что был мёртвым для `SpringImportOrderCheck` — для голого `ImportOrderCheck` не перепроверено, должен работать)
- **2026-08-20, `@Nested`+`@ActiveProfiles`-тесты реализованы на `JDBC`** — `UserNoteApplicationDefaultTest` → 3 `@Nested` (`H2`/`MySQL`/`PostgreSQL`); `MySQL`/`PostgreSQL` несут `@Testcontainers`+`@Container` static-поле **внутри `@Nested`-класса** (первый такой случай в проекте — работает благодаря relaxed-правилу Java 16+/JEP-JDK-8254936: static-поля в non-static inner-классах разрешены); ассерт на `dataSource.getConnection().getMetaData().getDatabaseProductName()` вместо голого `contextLoads()`, чтобы поймать тихий откат на H2-fallback. `build.gradle.kts` дополнен `com.example.spring-boot-testcontainers`/`-mysql`/`-postgresql`. Прогнано вживую против реальных Docker-контейнеров (Docker Desktop пришлось стартовать) — зелёный с первого прогона. Побочная находка: FQN контейнерных классов в этой версии Testcontainers — `org.testcontainers.mysql.MySQLContainer`/`.postgresql.PostgreSQLContainer` (не устаревший `.containers.*`), не generic (diamond-оператор неприменим). Побочный инцидент — `spotlessApply` и `SpringImportOrderCheck` конфликтовали на первом в проекте статическом импорте (third-party группа `*` неделима); обошли квалифицированным вызовом без статического импорта, build-логику не трогали (см. следующую запись выше по хронологии — explicit-список решает и эту проблему)
- **2026-08-20, первая реальная реализация — вендорные properties на `JDBC`** — единственный из вариантов (а)/(б)/(в)/(г) выбора вендора, реализованный первым (вариант (а), Spring profile), только на `user-note/application/webmvc/`: `application.properties` (общий) — только `spring.mvc.problemdetails.enabled=true`; `application-h2.properties` — `spring.h2.console.enabled=true` (вынесен как H2-специфичный концерн — H2 console имеет историю unauthenticated RCE, CVE-2021-42392, не открывать под `mysql`/`postgresql`-профилем); `application-{mysql,postgresql}.properties` — `spring.datasource.url=jdbc:{mysql,postgresql}://localhost:{3306,5432}/usernotedb`+creds, по образцу `user/application/application-{mysql,postgresql}-jdbc/`. `check` зелёный, дефолтный (без профиля) `contextLoads()` — по-прежнему на embedded H2-fallback. Сознательно не сделано в рамках этой правки: `spring.sql.init.mode`/`-platform` не заданы (см. апдейт 2026-08-18 ниже про `RANDOM_UUID()`); реального сервера профиль сам не поднимает (`spring-boot-docker-compose` готов, но не подключён); `@Nested`+`@ActiveProfiles`-тесты ещё не добавлены (см. следующую запись). Применимость к остальным 4 модулям не рассматривалась — правка scoped только на `JDBC`
- **2026-08-20, «все комбинации одним `clean check`»** — механизм найден: JUnit5 `@Nested`-классы, каждый со своим `@ActiveProfiles`, внутри одного `*ApplicationTests` (сверено с `docs.spring.io` → support-classes, задокументированный паттерн — пример `EnglishGreetings`/`GermanGreetings`). Каждый `@Nested` получает свой закешированный `ApplicationContext` (Spring TestContext кеширует по уникальному набору конфигурации), но это обычные `@Test`-методы в одном `test`-sourceSet — `clean check` прогоняет все сам, без `--tests`/тега/отдельной задачи. `@Tag` на `@Nested`-классе по-прежнему работает для последующей фильтрации по скорости (комбинируется с `includeTags`/`excludeTags`), но не обязателен для самой цели «одна команда — все комбинации»
- **2026-08-20, «ноль дублирования» — предел разобран** — единственное свойство, от которого при цели «0 дублирования» пришлось бы отказаться навсегда — «не тащить лишние технологии на classpath» (физика JAR: статичный набор классов не переключается по значению после сборки, не ограничение мастерства с Gradle). Ключевое уточнение — `@Profile` можно вешать на `@Configuration`/`@Bean` в самом composition-root, не на класс адаптера (сверено с `docs.spring.io` → environment.html — задокументированный идиоматичный паттерн: несколько `@Bean`-методов с одним `@Bean("...")`-именем и разным `@Profile` в одном `@Configuration`; адаптеры остаются framework-agnostic POJO, `@Service`/`@Component` с них нужно снять во избежание двойной регистрации бина). Предел для `user-note/application/` — максимум **2 модуля**: **Sync** (JDBC+JPA+MongoDB через `@Profile("jpa"/"jdbc"/"mongodb")` в одной конфигурации — Mongo не конфликтует, у неё нет `DataSource`) и **Reactive** (R2DBC+MongoDB-reactive, тот же принцип). Вендор внутри JDBC/JPA/R2DBC — отдельно решённая ось. Sync и reactive объединить нельзя ни при какой цене — `WebApplicationType.deduceFromClasspath()` (проверено по API Boot 4.1.0) всегда выбирает `SERVLET` при одновременном наличии webmvc+webflux на classpath, решается до обработки `Environment`/профилей вообще (обходится только ручным `setWebApplicationType()` в `main()` — не рекомендовано, не `@Profile`-механизм). Итог: 2 — практический потолок, не 1
- **2026-08-20, Gradle-«профили», попытка слить JDBC+JPA — отклонена** — идея выбирать `id("com.example.spring-boot-database-{...}")` через Gradle property внутри `plugins{}` не работает: блок декларативный, не читает `project.findProperty()`/`providers.gradleProperty()` (сверено с `docs.gradle.org` → plugins.html). Идея слить JDBC+JPA тем же способом, что вендор — отклонена как качественно другой случай: у вендора один и тот же бин просто подключается к разному `spring.datasource.url` (профиль меняет только конфигурацию), у JDBC/JPA — два физически разных бина на один порт, совместное присутствие на classpath даёт `NoUniqueBeanDefinitionException` без `@Profile` на адаптерах, чего в проекте нет нигде — новый паттерн, не масштаб старого; граница «сливать конфигурацию — не сливать код» подтверждена как уже корректная у нынешних 5 модулей. Альтернатива — условная `project()`-зависимость через `dependencies{}` + `-P` реально снимает bean-ambiguity, но платит: это выбор на этапе **сборки**, не рантайма (один `.jar` не переключается `--spring.profiles.active`, нужна отдельная сборка на комбинацию), и корневой `clean check` перестаёт агрегировать все комбинации одной командой бесплатно. Официально подтверждено (`docs.gradle.org` → build_environment.html, `docs.spring.io` → gradle-plugin/running.html) — Gradle-«профилей» не существует, вместо этого **project properties**, 4 источника по приоритету: `-P` → `-D org.gradle.project.*` → `gradle.properties` → `ORG_GRADLE_PROJECT_*` (для CI-секретов); официальный способ чтения — `providers.gradleProperty(...)` (Provider API). Для выбора Spring-профиля на `bootRun` — штатно без Gradle-property вовсе: `./gradlew bootRun --args='--spring.profiles.active=mysql'`. Итог: Gradle property (вопрос сборки) и Spring-профиль (вопрос рантайма) — разные уровни, не конкурируют
- **2026-08-20, профили для запуска приложения, не только тестов** — пользователь уточнил: переключение вендора нужно и для `bootRun`/деплоя, не только тестам. Testcontainers — инструмент только для тестов, не годится для `bootRun`. Для запуска — Spring profile (`spring.profiles.active=mysql`, реальный `spring.datasource.url`) + уже готовый `com.example.spring-boot-docker-compose` (прецедент — MongoDB уже использовал Testcontainers и Docker Compose вместе, не как альтернативы: Compose — dev-удобство, Testcontainers — то, что реально делало тесты зелёными). **Важное следствие** — раз `bootRun` с одного артефакта должен переключать вендор через профиль, это само по себе требует всех 3 драйверов на classpath одновременно (Gradle-зависимости не выбираются по рантайм-профилю) — объединённый 5-модульный вариант из 2026-08-19 не просто упрощает тесты, а необходим для самой возможности такого запуска, не только «меньшее зло». Несколько осей профиля одновременно подтверждены (`spring.profiles.active=mysql,dev`, конфликты — последний в списке побеждает, `spring.profiles.group.*` для частых комбинаций). Сверено с `docs.spring.io` → profiles.html: `spring.profiles.include` встаёт ПЕРЕД `active` (не просто «расширяет» — в начало списка, важно для порядка перекрытия); `active`/`-include`/`-group` нельзя объявлять внутри профиль-специфичного документа; имя профиля валидируется (буквы/цифры/`- _ . + @`, `spring.profiles.validate=false` снимает). Gradle не знает о Spring-профилях вообще — `compileJava`/`bootJar`/checkstyle не видят профиль, паковка нейтральна; профиль участвует только в `test`-задаче через тестовый код. Проверка конкретного профиля на `clean build` = `clean` + тесты, отфильтрованные/сконфигурированные на профиль (разовый `--tests`, именованная `Test`-задача вне `tasks.check`). **Решено**: тестовая матрица — только ось вендора (H2/MySQL/PostgreSQL + Mongo/Mongo-reactive), без комбинаторики с env-осью (`dev`/`test`/`deploy` — конфигурация запуска, не предмет автотеста). Как поймать тихий откат на H2-fallback — ассерт на `getDatabaseProductName()`/`ConnectionFactoryMetadata.getName()` внутри самого теста, не факт зелёного `contextLoads()` (реализовано следующим шагом, см. выше)
- **2026-08-19, механика shared-тестов + driver bloat принят сознательно** — shared-тестовая механика для голого `contextLoads()` подтверждена по первоисточнику (`docs.gradle.org` → java_testing.html#test-fixtures: `testFixturesApi`/`-Implementation`, `testImplementation(testFixtures(project(...)))`) и живому прецеденту `spring-boot` (`spring-boot-jdbc` `dockerTest`-sourceSet — расшаривает только инфраструктуру `@DockerComposeTest`-аннотации, не сам сценарий/assertions, у Postgres/MySQL сценарии генуинно разные; отдельный, свой Gradle sourceSet, не гоняется штатным `check`); побочно найден паттерн `spring-data-relational` — «1 сценарий, много БД» через 1 модуль + `-Dspring.profiles.active=<vendor>`, не через module-per-vendor. Вывод: у `contextLoads()` нет легитимного вендорного расхождения, полное расшаривание (абстрактный класс + пустой `extends`) не противоречит практике Spring Boot. **Параллельно, реализовано пользователем вручную (не ассистентом)**: 11 composition-root модулей `user-note/application/{Tech}/{Tech}-{Vendor}/` сжаты до 5 плоских — `application/{JDBC,JPA,R2DBC,MongoDB,MongoDB-reactive}/`; на `JDBC`/`JPA`/`R2DBC` теперь ВСЕ 3 вендорных convention-плагина одновременно (не по одному на модуль); код взят из бывшего `H2`-варианта, `MySQL`/`PostgreSQL`-код (был идентичен по вендорам) удалён как дубликат. **Прямо противоречило решению 2026-07-24 «вендор — граница модуля»** (driver bloat возвращается) — уточнено тем же вечером как осознанный выбор, не промежуточный шаг: альтернатива (сохранить 33 вендорных модуля, дедуп тестов через test-fixtures) явно отклонена пользователем, driver bloat принят как меньшее зло против сложности test-fixtures-инфраструктуры; **не постоянное решение** — пользователь допускает возврат к разделению по вендору позже специально ради driver bloat. `note`/`user` физически не тронуты. `clean check` зелёный на 5 модулях; эмпирически подтверждено — embedded-fallback (H2) продолжает резолвиться даже при 3 вендорных драйверах одновременно на classpath
- **2026-08-18, архитектура тестирования для TDD-переписывания — принцип решён** — sociable/classicist-стиль (реальный Spring-контекст, реальные бины, без Mockito/`@MockitoBean`, embedded H2/Mongo вместо реального вендора, пока возможно). Тесты живут на уровне application/composition-root модулей, НЕ в `presentation/webmvc|webflux`/`persistence/data-*` (те остаются 0 тестового кода) — acceptance-testing аналогия, симметрична принятой архитектуре DI (порт — структурный контракт, связывание и теперь верификация поведения — на application-уровне). Закрывает 2 открытых пункта: «поведенческая аналогичность» п.1 «порты не специфицируют поведение» (теперь осознанное разделение, не пробел) и «unit-тесты бизнес-логики» из «Зрелость CRUD-модулей» (решено — не нужны отдельно). Дублирование сценария между application-модулями разрешено по типу: (1) семантическое (сценарий/assertions) — устраняется shared-модулем с абстрактным JUnit-классом + тонкий подкласс (2–3 строки) на модуль; 2 такие семьи — sync (JDBC×3+JPA×3+MongoDB = 7 подклассов) и reactive (R2DBC×3+MongoDB-reactive = 4), физически вне presentation-модулей; имя/расположение shared-модуля и convention-плагин (нужна `api`-транзитивность JUnit/Spring-test-типов, аналог `java-library`) — не выбраны, не реализовано; (2) структурное (`@SpringBootApplication`-класс на модуль) — не устраняется, требование самого Spring Boot, цена принята по тому же прецеденту, что и диамант-правило
- **2026-08-18, падение `contextLoads()` на 6 vendor-модулей — решено проще, чем рассмотренные опции 2026-08-16** — `contextLoads()` не обязан поднимать реальный вендор. `testRuntimeOnly(h2/r2dbc-h2)` добавлен в 4 файла `build-logic/` (`spring-boot-database-mysql`/`-postgresql`/`-r2dbc-mysql`/`-r2dbc-postgresql`) — `application.properties` этих модулей не задаёт URL, поэтому автоконфигурация fallback'ится на embedded H2 (тот же механизм, что уже делал H2-варианты зелёными). `runtimeOnly`-драйвер вендора остаётся нетронутым (prod `bootJar` не подхватывает H2, `testRuntimeOnly` не просачивается). `clean check`/`clean build` зелёные на всех 11 (на тот момент) модулях. **Не решено этим изменением**: `contextLoads()` больше не проверяет реальный вендор-диалект вообще — отдельный, не smoke-уровня тест на это не написан. **Уточнено тем же вечером** (по вопросу «зачем вообще Testcontainers, что он проверит») — план «когда persistence/presentation будет довязан, заменить `testRuntimeOnly(h2)` на реальный Testcontainers» недостаточен сам по себе, расходится по технологии: JPA — Hibernate валидирует DDL по реальным метаданным при старте, замена `DataSource` достаточна; JDBC/R2DBC (`schema.sql`) — НЕТ, `spring.sql.init.mode` по умолчанию `embedded`, `schema.sql` не выполнится против настоящего вендора без явного `spring.sql.init.mode=always` (нигде не задано в истории репозитория) — доказательство расхождения: `RANDOM_UUID()` в текущем `schema.sql` специфична для H2 (MySQL — `UUID()`, PostgreSQL — `gen_random_uuid()`). Уточнённый план для JDBC/R2DBC на будущее: замена `DataSource` на Testcontainers **и** `spring.sql.init.mode=always` **и** развод `schema-{vendor}.sql`+`spring.sql.init.platform` — иначе тест останется почти пустым ритуалом
- **2026-08-16, падение `UserNoteApplicationDefaultTest` на 6 модулей — причинный разбор, решение не принято** — `contextLoads()` расставлен на все 11 skeleton-модулей, 6 падают (`{JDBC,JPA,R2DBC}-{MySQL,PostgreSQL}`, `DataSourceBeanCreationException`) — вендорный плагин тянет реальный драйвер и требует подключения, а persistence/presentation ещё не довязаны, Testcontainers/URL не заданы; H2 — embedded-fallback, MongoDB — нет `DataSource`. Установлено: `DataSourceAutoConfiguration` реагирует на classpath, не на использование; `contextLoads()` как уровень теста — корректен (единственная цель composition-root модуля — проверить, что весь стек поднимается вместе). Оси решения перечислены, часть отклонена: mock/`exclude` (запрещённая заглушка), in-memory H2 вместо вендора (обессмысливает вендорный модуль), постоянная внешняя БД (непригодна — не изолирована/не воспроизводима), убрать вендорные плагины (откат к примитивному, запрещён). Testcontainers vs Docker Compose сравнены — Testcontainers готов «как есть» в `build-logic/`, Docker Compose по умолчанию отключён во время тестов (`spring.docker.compose.skip.in-tests=false`+`testAndDevelopmentOnly` нужны отдельно). Доп. категории найдены при повторном проходе: CI-managed service container (эфемерно на уровне CI, не решает локальный `test`), embedded native binary (`io.zonky` для PostgreSQL — жив, `wix-embedded-mysql` — статус сомнителен), ось «когда запускать» (JUnit-теги/отдельная задача — ортогональна источнику `DataSource`). Решение снято позже 2026-08-18 (см. выше)
- **2026-08-15, вендорные + технологический + presentation convention-плагины добавлены на 11 skeleton-модулей** (пользователь вручную, через IDE) — вендорные (`spring-boot-database-{h2,mysql,postgresql}`/`-r2dbc-*`) на 9 реляционных модулей; 2 несуществующих id на MongoDB-модулях исправлены агентом (`com.example.spring-boot-data-mongodb[-reactive]`, не `-database-mongodb*` — у Mongo нет отдельного driver-плагина). Технологический плагин (`spring-boot-data-{jpa,jdbc,r2dbc}`) добавлен рядом с вендорным. Старые 11 `application-{vendor}-{tech}/`-модулей (полный код, вне сборки) удалены целиком (`git rm`) — закрывает открытый вопрос «судьба старых модулей». Presentation-плагин (`spring-boot-webmvc`/`-webflux`) добавлен по sync/reactive оси на все 11; `application.properties` доведены до фактической технологии каждого модуля (`problemdetails` разведён по webmvc/webflux, `spring.h2.console.enabled` — только на sync+H2, снят с `R2DBC-H2` как Servlet-based UI). `clean check` зелёный (NO-SOURCE везде — кода в модулях всё ещё нет)
- **2026-08-14, старт переписывания `user-note/application/`** — `auth/`·`config/`·`gateway/`·`registry/` удалены из репозитория целиком (`git rm`) — причина/постоянство не выяснены. `user-note/application/` начал переход с плоских `application-{vendor}-{tech}/` (полный код) на вложенные `application/{Tech}/{Tech}-{Vendor}/` (11 новых пустых skeleton-модулей; старые физически остались на диске, но исключены из сборки). `note/`·`user/` целиком исключены из `settings.gradle.kts` (код не тронут) — временно/навсегда не выяснено. Агент исправил `settings.gradle.kts` под вложенные пути (`include(":user-note:application:{Tech}:{Tech}-{Vendor}")` — сегменты совпадают с диском, `projectDir`-переопределение не нужно). `clean compileJava` зелёный (NO-SOURCE). Коммит `373e636`. Открыто на момент записи (закрыто последующими апдейтами выше, кроме (4)): (1) порядок `{Tech}-{Vendor}` против решения 2026-07-24 «вендор первым» — снят как неактуальный 2026-08-19 (см. «Именование» → «Vendor»); (2) судьба старых 11 модулей — закрыто 2026-08-15 (удалены); (3) вернутся ли `note`/`user` без изменений — по-прежнему не выяснено; (4) причина удаления 4 скелетных сервисов — по-прежнему не выяснена, не предполагать ответ

### Завершено (закрытые темы, хронология, новое сверху)

- **2026-09-04, Checkstyle/Spotless возвращены на дефолт инструментов, база — Google Checks** (явный запрос пользователя «настрой всё по дефолту, основа Google», после сверки с официальной докой через WebFetch/WebSearch, не по памяти) — заменяет весь стек решений ниже по хронологии, построенных на Spring-бандле (`io.spring.javaformat.checkstyle.SpringChecks`): `gradle/checkstyle/checkstyle.xml` теперь дословная копия `google_checks.xml` (вытащен из реально установленного `checkstyle-13.10.0.jar`, не с сайта) с ровно двумя переопределениями — `LineLength.max` 100→120, `Indentation.basicOffset` 2→4, всё остальное как в бандле по прямому указанию пользователя «не указывай лишнего». Тем самым сняты как неприменимые (база сменилась, класса чеков, под которые они были написаны, больше нет): флип `SpringLambdaCheck.singleArgumentParentheses` под SonarQube S1611, допуск `{}` без пробела через `WhitespaceAroundCheck`, запрет пустой строки перед `}` (`RegexpMultilineCheck`), explicit-группы `ImportOrderCheck` — убраны полностью по явному решению пользователя, не перенесены поверх новой базы. `com.example.codequality-checkstyle.gradle.kts` — убрана мёртвая зависимость `spring-javaformat-checkstyle`; `spring-javaformat`-версия убрана из `gradle/libs.versions.toml` (использовалась только там). `com.example.codequality-spotless.gradle.kts` переписан на `java { googleJavaFormat().aosp() }` — `.aosp()` даёт 4-пробельный отступ (подтверждено официальным README `google-java-format`), пара с новым `Indentation.basicOffset=4`; `lineEndings=PRESERVE` снят (дефолт `GIT_ATTRIBUTES_FAST_ALLSAME`); привязка `compileJava.dependsOn(spotlessApply)` снята — действует штатная `spotlessCheck` внутри `check` (не чинит, только валит сборку на нарушении). **Побочный эффект возврата к дефолту `lineEndings`**: он вскрыл реальное несоответствие — исходники `user-note/` на диске были в CRLF (локальный `core.autocrlf=true`, см. запись 2026-08-29 про `.gitattributes`), хотя `.editorconfig` требует LF — `./gradlew spotlessApply` прогнан один раз, нормализовал. **Важное следствие, требующее отдельного решения пользователя (не поднято/не решено в рамках этой правки)**: у `google_checks.xml` severity по умолчанию — `warning`, не `error` (`${org.checkstyle.google.severity}` default `warning`), а у Gradle Checkstyle-таска `maxWarnings` по умолчанию `Integer.MAX_VALUE`/`maxErrors` — `0` (проверено `docs.gradle.org`) — то есть **в чистом дефолте Checkstyle-нарушения сборку `check` больше не валят вообще**, только репортятся в HTML/XML-отчёт. Раньше (Spring-бандл, severity не переопределялась = `error` по умолчанию самого Checkstyle) любое нарушение падало сборку. Оставлено как есть по букве запроса «всё по дефолту, если можно не указывать — не указывай» — не менял самостоятельно, не архитектурное решение агента. `./gradlew clean check --continue` прогнан дважды: все `checkstyleMain`/`checkstyleTest`/`spotlessCheck`-таски зелёные на всех 5 модулей; 4 упавших теста (`MySQL`/`PostgreSQL`-ветки `application-{jdbc,jpa,r2dbc}`+один в `mongodb-reactive`) — `DockerClientProviderStrategy`, Docker Desktop не запущен на момент прогона, не связано с этой правкой. **В тот же вечер** — по прямому запросу «как сделать чтобы spotlessApply применялся автоматически» — `compileJava.dependsOn("spotlessApply")` возвращён в `com.example.codequality-spotless.gradle.kts` (та же строка, что была снята днём тем же днём ради «дефолта») — автофикс снова срабатывает сам при любой сборке, не только `spotlessCheck`-проверка.
- **2026-09-04 (вечер), `id("org.springframework.boot")` убран пользователем из `spring-boot-application.gradle.kts` и `spring-cloud-application.gradle.kts` — нигде в графе convention-плагинов Gradle-плагин `org.springframework.boot` больше не применяется, и это НЕ ломает `clean check`** — источник путаницы «почему всё работает», разобран и проверен empирически, не только рассуждением. Механизм: рантайм Spring (`@SpringBootApplication`/`SpringApplication.run()`/`@SpringBootTest`/DI/автоконфигурация) — обычный Java-код из зависимости `org.springframework.boot:spring-boot-starter(-test)` (`implementation(...)` в `com.example.spring-boot.gradle.kts`), не имеет отношения к самому Gradle-плагину и работает независимо от того, применён ли он. Сам плагин `org.springframework.boot` отвечает только за Gradle-таски **упаковки**: `bootJar` (executable/fat jar с зависимостями внутри и `Main-Class`/`Start-Class` в манифесте), `bootRun`, `bootBuildImage` — и ничего больше. `check` (и все таски под ним — `test`/`checkstyleMain,Test`/`spotlessCheck`) не зависит от `bootJar`/`assemble` вообще, поэтому отсутствие плагина никак не проявляется в `clean check`. Проверено: `./gradlew :user-note:application-jdbc:bootJar` → `task 'bootJar' not found`; `./gradlew :...:build` при этом «успешен», но вместо `bootJar` собирает голый `jar` — `application-jdbc.jar`, 993 байта/8 файлов (только `.class`+`package-info.class`+`application.properties`, НИ ОДНОЙ зависимости внутри), `MANIFEST.MF` содержит только `Manifest-Version: 1.0` (ни `Main-Class`, ни `Start-Class`) — такой jar нельзя запустить `java -jar` вообще. **Открытый вопрос, не решён**: если убор `id("org.springframework.boot")` — осознанное решение (не диамант-устранение, поскольку с 2026-08-01 диамант там уже был устранён другим путём — родитель `spring-boot-application` применял и `com.example.spring-boot`, и напрямую `org.springframework.boot`, оба вели к разным вещам, не к общему предку) — тогда `bootJar`/`bootRun` нужно вернуть каким-то другим способом (например явным `id("org.springframework.boot")` на каждом из 5 листовых `user-note/application-*`, по аналогии с `spring-boot-actuator`), иначе ни один из 5 модулей нельзя реально собрать в исполняемый jar или запустить `bootRun`. Текущее состояние на диске (2026-09-04, вечер): в `spring-boot-application.gradle.kts` строка **закомментирована** (`//    id("org.springframework.boot")`, не удалена физически), в `spring-cloud-application.gradle.kts` — удалена полностью; практический эффект одинаков (плагин не применяется нигде), различие чисто текстовое.
- **2026-09-04 (вечер), регрессия и фикс — `testRuntimeOnly("com.mysql:mysql-connector-j")` в `com.example.spring-boot-testcontainers-mysql.gradle.kts`** — при дедупликации зависимостей (см. запись выше про `google_checks.xml`/убор `id("org.springframework.boot")`, тот же проход) эта строка была снята как «избыточная» — предполагалось, что JDBC-драйвер MySQL и так приходит транзитивно через `runtimeOnly` из `com.example.spring-boot-database-mysql` (`testRuntimeOnly` по умолчанию `extendsFrom runtimeOnly` в стандартном Java-плагине Gradle). Верно для `application-jdbc`/`application-jpa` (оба применяют `spring-boot-database-mysql`), но **не для `application-r2dbc`** — тот применяет `spring-boot-database-r2dbc-mysql` (другой драйвер, `io.asyncer:r2dbc-mysql`), JDBC-драйвера `com.mysql:mysql-connector-j` там больше не было ни от кого. Testcontainers `MySQLContainer` для собственной wait-strategy всегда требует именно JDBC-драйвер на classpath, независимо от того, каким способом сам модуль ходит в БД (тот же факт уже задокументирован 2026-07-23 — `PostgreSQLContainer`, для сравнения, log-based и JDBC-драйвер не требует). Симптом — `application-r2dbc:test` падал `JdbcDatabaseContainer$NoDriverFoundException` → `ClassNotFoundException` (не Docker-проблема, отличить по стектрейсу: `NoDriverFoundException`, не `DockerClientProviderStrategy`). Строка возвращена пользователем — `clean check` снова зелёный на всех 5 модулях, включая `application-r2dbc:test`. Урок: `runtimeOnly`→`testRuntimeOnly`-наследование зависит от того, какой ИМЕННО driver-плагин применён в конкретном листовом модуле — не одинаково по всей матрице технологий, дедуплицировать такие зависимости нужно с проверкой per-модульного состава `plugins{}`, не по одному примеру.
- **2026-08-16, Spotless для порядка импортов** — новый плагин `com.example.codequality-spotless` (Spotless 8.9.0) — `importOrder("java","javax","","org.springframework")`, та же группировка, что `SpringImportOrderCheck`, но с автофиксом (`spotlessApply`). `lineEndings = LineEnding.PRESERVE` выставлен явно (дефолт Spotless требует LF, конфликтовал бы с `.gitattributes`/локальным `core.autocrlf`). `compileJava` зависит от `spotlessApply` — фиксит импорты автоматически при любой сборке, без git-хука; работает одинаково для любой машины/IDE. Верифицировано вживую дважды. Закрывает пункт «порядок импортов» из «Открытые решения» → «Качественная настройка Checkstyle/codequality»; заменён позже 2026-08-20 на голый `ImportOrderCheck` с явным списком групп (см. «Активная нить» выше) — сам механизм (Spotless-автофикс + `compileJava`-зависимость) не изменился; оба (explicit-группы и автофикс-на-`compileJava`) сняты 2026-09-04 при возврате на дефолт `google-java-format`, см. «Завершено» → «2026-09-04»
- **2026-08-15, code quality «золотой стандарт»** — 7 правок: `spring-javaformat` `0.0.48-SNAPSHOT`→стабильный `0.0.48` (снимает риск снапшота, принятый 2026-07-13); Checkstyle 13.8.0→13.10.0; jspecify 1.0.0→1.0.1; мёртвый `configProperties` убран из `com.example.codequality-checkstyle` (перепроверено декомпиляцией — безопасно); `org.gradle.toolchains.foojay-resolver-convention` добавлен в `settings.gradle.kts` (автозагрузка JDK на чистой машине); `distributionSha256Sum` добавлен в `gradle-wrapper.properties`; новый `.editorconfig` в корне (4 пробела/LF/UTF-8/final newline/trim trailing whitespace). Все версии сверены через `gh api` (GitHub releases), не поиском. `checkstyle.xml` — мёртвый `JavadocStyleCheck`-excludes убран (чек удалён из `SpringChecks` 0.0.48, заменён `SummaryJavadocCheck`+`MissingJavadocPackageCheck`); `SpringMethodOrderCheck`/`SpringNullabilityCheck` перепроверены байткодом — не новое поведение, `SpringMethodOrderCheck` проверяет строго `equals→hashCode→toString`, не решает вопрос «порядок методов = порядок в интерфейсе». Найден и задокументирован (не исправлен — «используем позже») `com.example.codequality-jacoco-report-aggregation` как ничего не агрегирующий; **уточнено в тот же день** — не баг: `jacocoAggregation` не нужно объявлять явно при наличии `jvm-test-suite` (есть у нас через `java`), плагин настроен верно, агрегировать пока нечего (skeleton без зависимостей/тестов), включится сам, когда в composition-root появятся project-зависимости и тесты. Также: junit-jupiter/platform 6.1.2→6.1.3; 3 новых плагина `spring-boot-batch`/`-batch-jdbc`/`-batch-data-mongodb` (id сверены по `start.spring.io/metadata/client`, первая технология в проекте без предыдущих упоминаний); голые `spring-boot-starter-jdbc`/`-r2dbc`/`-mongodb`(+test) убраны из `data-jdbc`/`-r2dbc`/`-data-mongodb` — доказанная избыточность (POM `data-*`-стартеров Boot 4.1.0 уже тянет их транзитивно, 0 использований голого API в продакшн-коде, верифицировано и по POM, и эмпирически); `data-elasticsearch` не тронут (обратный случай — голый стартер несёт реальный драйвер, которого иначе нет). `clean check` зелёный (только 11 skeleton-модулей `user-note` в сборке на тот момент)
- **2026-08-08, единый generic-контракт Controller/Service/Repository** — 18 файлов интерфейсов переписаны по всем трём сервисам, sync+reactive: `{Entity}Interface<B, S>` (без `L`) для `existsById`/`add`/`findById`/`replace`/`merge`/`remove`; `findAll()` и entity-специфичные finder'ы объявляются независимо, конкретным bounded-типом, в каждом из 4 интерфейсов — `L` доказанно неустраним как свободный generic-параметр (инвариантность generics: `ResponseEntity<List<X>>` не подтип `ResponseEntity<Iterable<X>>`, `Flux<T>` не `Iterable`, проверено компилятором/исходниками `reactor-core`, не предположено; полная история отклонённых вариантов — кодогенерация, HKT-эмуляция, отказ от `ResponseEntity`, GoF Adapter — см. `docs/decisions-log.md` → «Архитектура»). Ни один класс-реализация (37 driven-адаптеров, 6 контроллеров) не тронут — точечный `compileJava` подтвердил идентичность сигнатур. Попутно исправлены 7 pre-existing нарушений Checkstyle в `user-note/` (wildcard-импорты). `clean check` зелёный кроме известного блока Docker Hub CDN
- **2026-08-02, независимая программная верификация графа convention-плагинов** — впервые проверка сделана программным построением графа `com.example.*`→`com.example.*` по всем 49 файлам `build-logic/convention/` (не ручным grep, который дважды ошибался ранее). Результат: 0 диамантов, все 7 листовых сервисов чисты (только `id("com.example.*")` + только `project()`-зависимости), `docs/convention-plugins-graph.md` сверен без расхождений. **Тем же вечером, при более глубокой проверке** (числа применений против диска, не только структура) — найдена и исправлена реальная регрессия: `com.example.spring-boot-actuator` документирован с 37 применениями, но фактически 0 — коммит `283ff49` (откат диамант-эксперимента 2026-08-01) заодно стёр `id("com.example.spring-boot-actuator")` со всех 37 листовых `build.gradle.kts` (слишком широкий `git checkout`, затронул 40 файлов вместо предполагавшихся 9). Восстановлено из `283ff49~1`. **Новое правило в «Правила»**: после широкого отката, затрагивающего convention-плагины, сверять число применений грепом против `docs/convention-plugins-graph.md`, не полагаться только на зелёный `clean check` (отсутствие неиспользуемого стартера не ломает компиляцию)
- **2026-08-02, первая полная `clean check`-верификация после actuator-регрессии** — 1-й прогон дал ложные сбои (класс не найден для существующего кода, `EOFException`, `FileNotFoundException` на `jacoco/test.exec`) — расследование (`ps aux`/`--status`) показало два одновременно активных Gradle-демона на одном репозитории, писавших в одни `build/`-директории. После остановки постороннего — 401/401 задач зелёных (после точечного перезапуска 3 упавших от гонки `user-note:application-mysql-*:test`). **Новое правило**: при необычном инфраструктурном сбое (класс не найден, `EOFException`, `FileNotFoundException` на служебных файлах, без assertion) — сначала проверить посторонние демоны, прежде чем считать сбой регрессией кода
- **2026-08-01, четыре связанные правки convention-плагинов и структуры модулей**: (1) все модули сгруппированы по слою в подпапку (`application/domain/persistence/presentation`) во всех 3 сервисах — Gradle project path остался плоским, физическая директория переопределена через `projectDir` в `settings.gradle.kts`; (2) атомизация базовых плагинов — `base`→`java`, `library`→`java-library`, `codequality`-агрегатор убран (5 фрагментов подключены напрямую в `java`), новый атомарный `spring-boot-validation` (только 9 модулей с реальным автотриггером — `webmvc`/`webflux`/`data-jpa` × 3 сервиса), `reactor`→`project-reactor`, `spring-boot-actuator` воссоздан как явная композиция на 37 листах (не диамант — расхождения в потребности нет, 100% из 37 хотят actuator, но пользователь всё равно выбрал явную композицию вместо диаманта); (3) диамант `spring-cloud-application` устранён (прямой `id("org.springframework.boot")` вместо `spring-boot-application`, возможно только благодаря (2)), gateway-плагины переименованы под точное имя артефакта (`-gateway-server-webflux/webmvc`), `spring-cloud-*-server`-плагины приведены к общему паттерну (родитель — только BOM, bootable — явная композиция на листе, без единого исключения по всему графу); (4) правило про диаманты сделано абсолютным «всегда устранять» (после того как пользователь откатил эксперимент со встраиванием actuator вторым родителем — воссоздание диаманта ради компактности, отменил сразу же; пользователь дважды пробовал сознательно принять диамант ради экономии кода — здесь и 2026-07-15 — и оба раза откатывал обратно к нулю), `.gitattributes` зафиксирован как точная копия шаблона Spring Initializr (LF/CRLF-предупреждения от `core.autocrlf` игнорировать). `clean check --continue` — стабильно 24 известных Docker-падения, без новых, после каждого шага
- **2026-07-24, четыре темы**: реструктуризация `docs/` (6 справочных файлов перенесены из корня, AI-only форматирование по всему `CLAUDE.md`/`docs/decisions-log.md` — оглавление убрано, ASCII-таблицы→плоские списки, относительные отсылки→конкретные якоря); порядок имени composition-root перевёрнут с `{tech}-{vendor}` на `{vendor}-{tech}` (директории/классы/`settings.gradle.kts`/`docs/file-catalog.md` обновлены); первая реальная сборка в remote execution environment (`apt-get install openjdk-25-jdk-headless` в обход заблокированного `foojay`/`start.spring.io`) — 151/154 задач, 3 падения из-за блокировки Docker Hub CDN сетевой политикой окружения (задокументировано в «Правила» → «Build»); **вендор БД — граница Gradle-модуля** (главное архитектурное решение дня, детали — `docs/decisions-log.md`) — driver bloat (один `bootJar` тащил H2+MySQL+PostgreSQL одновременно) устранён разбиением `application-jpa`/`-jdbc`/`-r2dbc` на 9 вендорных модулей каждый (15→33 composition-root модуля по 3 сервисам), верифицировано напрямую по `dependencies --configuration runtimeClasspath` (ровно один драйвер), r2dbc-плагины переименованы для sort-by-name-группировки
- **2026-07-23, четыре темы**: сверка версий стека с GitHub releases API (NullAway 0.13.7→0.13.8 — поддержка Reactor); 16 из 48 convention-плагинов переименованы для sort-by-name-группировки (слово-маркер из суффикса в префикс — `{vendor}-database`→`spring-boot-database-{vendor}`; `codequality`-фрагменты получили общий префикс; `spring-boot-oauth2-*`→`-security-oauth2-*`; `client-web/-rest`→`-client-webclient/-restclient`); Testcontainers/Docker Compose возвращены симметрично на всю вендорную ось (было снято с MongoDB ради последовательности, затем решено обратное — довести PostgreSQL/MySQL до того же покрытия; попутно найден и исправлен баг Testcontainers — `MySQLContainer` требует JDBC-драйвер на classpath для wait-strategy, `PostgreSQLContainer` — нет, log-based); полная технологическая матрица введена впервые (5 composition-root модулей на сервис — `application-{jpa,jdbc,mongodb,r2dbc,mongodb-reactive}`, вендорная ось H2/PostgreSQL/MySQL для jpa/jdbc/r2dbc через рантайм-профили без новых Gradle-модулей — позже, 07-24, переведена на границу модуля)
- **2026-07-15**: диамант 4 standalone `spring-cloud-*`-плагинов сначала устранён линейным `spring-cloud-application`, затем пользователь пересмотрел на явную композицию 2 родителей (`spring-cloud`+`spring-boot-application`) — воссоздание диаманта осознанно, несмотря на явную рекомендацию агента в пользу линейного варианта (диамант был доказанно безопасен — идемпотентность Gradle `PluginManager`, баг gradle/gradle#13252 исключён); правило на тот момент — «сообщать всегда, решение за пользователем» (ужесточено до абсолютного «всегда устранять» 2026-08-01). Разграничены вид 1 (диамант внутри самого плагина — устранять) и вид 2 (лист применяет 2+ плагина, например `note/application-jpa` — не диамант, осознанная композиция, не трогать). `jspecify` вынесен из `nullaway` в отдельный плагин, `java-library`-каскад исправлен на точечный `java`+`implementation` (был протекал во все 39 модулей без надобности). Leaf-purity правило (только `id("com.example.*")`+`project()`) проверено и закреплено. Checkstyle 13.7.0→13.8.0
- **2026-07-14**: аудит циклов/диамантов по convention-плагинам/Gradle-зависимостям/Java-иерархии портов — не найдено (единственное на тот момент схождение, standalone `spring-cloud-*`, устранено 2026-07-15). `spring-boot-starter-validation`(`-test`)→`com.example.spring-boot`; `-actuator`(`-test`)→`spring-boot-application` (пересмотрено 2026-08-01 — actuator выделен обратно в атомарный плагин). `jakarta.validation-api:3.1.1` добавлен в `com.example.base` (аннотации на полях по-прежнему не расставлены). `{Entity}Persistable` — общий маркер-интерфейс в `domain/`, реализован всеми 15 model-классами; `user-note.role` унифицирован до `UserNoteRole` enum; `schema.sql` создан для `data-jdbc`/`data-r2dbc` во всех трёх сервисах. `data-jdbc/` переведён с сырого `NamedParameterJdbcTemplate` на Spring Data JDBC (`ListCrudRepository`) по всем трём сервисам
- **2026-07-13**: `io.spring.javaformat` (Gradle-форматер, не даёт переопределить `lineSplit`/`join_wrapped_lines`) убран — единственный гейт стиля стал Checkstyle (`SpringChecks`+5 личных модулей под 10 правил); `spring-javaformat`-checkstyle-addon запинен на `0.0.48-SNAPSHOT` (снят на стабильный `0.0.48` — см. «Завершено 2026-08-15» выше). Версии подняты до актуальных на тот момент (Java 25, Gradle 9.6.1, Spring Boot 4.1.0, JUnit 6.1.2, Checkstyle 13.8.0, reactor-core 3.8.6, jacoco 0.8.15). Переименования: трёхуровневая иерархия портов (`{Entity}Interface`/`ServiceInterface`/`ControllerInterface`), `data-contract`→`contract`, `application-reactive/` добавлен. `spring-boot-starter-r2dbc`/`-jdbc` (plain API) добавлены рядом с `data-r2dbc`/`data-jdbc`
- **2026-07-07**: пересмотр CRUD-сервисов — статус ГОТОВО подтверждён по `note`/`user`/`user-note`; оставшиеся расхождения — см. «Открытые решения»

### Статус по сервисам и модулям (актуализировано 2026-08-31)

- `note/`·`user/` — **удалены из репозитория целиком 2026-08-29 (вечер)** (`git rm`, намеренно, по подтверждению пользователя; ранее уже были вне `settings.gradle.kts`). Были зрелые: `domain·contract·contract-reactive·webmvc·webflux·data-{jpa,mongodb,jdbc,r2dbc,mongodb-reactive}` + 11 composition-root модулей каждый. Возврат — по отдельному решению, не предполагать
- `user-note/` — **весь зрелый гексагональный слой удалён 2026-08-29 (вечер)** (`domain/`·`persistence/`·`presentation/` — `git rm`, см. «Активная нить» → «2026-08-29 (вечер)»). Осталось **5 плоских composition-root модулей-скелетов** (без `dependencies {}`, без адаптеров/портов; все в `settings.gradle.kts`, `clean build` зелёный с Docker). Пакет/класс — `com.example.usernote.UserNoteApplication`, голый `@SpringBootApplication`. **⚠️ 2026-08-31 (вечер): 5 модулей = разные реализации одного и того же** (суть идентична, различия — только из-за технологии). Разделение осознанное/правильное (classpath — модули НЕ сворачивать). Следующая сессия выводит **одну абстракцию (макс 2: sync/reactive)** — `@SpringBootApplication`, сценарий теста, тест-инфраструктуру — в общее место, каждый модуль её реализует, а не переписывает; тест написан на абстракции; см. «Активная нить» → «2026-08-31 (вечер), разбор» и «Запланировано на следующие сессии» п. 0. Текущее состояние 5 модулей — тест каждого внешний `@SpringBootTest`+`@Import(UserNoteTestConfiguration)` + `@Nested @ActiveProfiles(...)`-ветки:
  - `user-note/application-jdbc/` — sync реляционный: webmvc+data-jdbc+`-database-{h2,mysql,postgresql}`+`-testcontainers-{mysql,postgresql}`. Ветки h2/mysql/postgresql через `abstract DataSourceTests` (`@Autowired DataSource`, `getMetaData().getDatabaseProductName()` `isEqualTo`)
  - `user-note/application-jpa/` — то же (тот же `abstract DataSourceTests`), `data-jpa` вместо `data-jdbc` (единственное различие с `application-jdbc/` сейчас; разделение по просьбе пользователя, «несмотря на почти-идентичность»)
  - `user-note/application-mongodb/` — sync Mongo: webmvc+data-mongodb+testcontainers-mongodb. `spring.mongodb.*` в `application-mongodb.properties`, `@Bean @Profile("mongodb") @ServiceConnection MongoDBContainer`. `abstract MongoTemplateTests` (`@Autowired MongoTemplate`, `executeCommand(buildInfo)` → `containsKey("version")`) + `@Nested @ActiveProfiles("mongodb") class MongoDB extends MongoTemplateTests {}`
  - `user-note/application-r2dbc/` — reactive реляционный: webflux+data-r2dbc+`-database-r2dbc-{h2,mysql,postgresql}`+testcontainers(+`-r2dbc`). `spring.webflux.problemdetails.enabled=true`; `spring.r2dbc.url=r2dbc:...` в `application-{mysql,postgresql}.properties` (h2 — без файла: embedded-fallback + `spring.h2.console` servlet-only, к webflux неприменимо). `abstract DatabaseClientTests` с `@Autowired org.springframework.r2dbc.core.DatabaseClient`, `databaseClient.inConnection(connection -> Mono.just(connection.getMetadata().getDatabaseProductName())).block()` → **`.contains(expected)`** (R2DBC product name НЕ нормализован как JDBC: MySQL-драйвер даёт `"MySQL Community Server - GPL"`; H2/PostgreSQL — ровно так)
  - `user-note/application-mongodb-reactive/` — reactive Mongo: webflux+data-mongodb-reactive+testcontainers-mongodb. Как `application-mongodb/`, но `abstract ReactiveMongoTemplateTests` с `@Autowired ReactiveMongoTemplate` + `.executeCommand(buildInfo).block()`
- `auth/`·`config/`·`gateway`·`registry/` — **удалены из репозитория целиком** (2026-08-14, `git rm`) — были СКЕЛЕТ (Eureka/Config/Gateway/Actuator подключены, `contextLoads` проходил, без routes/config-репозитория/реальной регистрации); возврат — по отдельному решению, не предполагать
- `graphql/`·Spring Cache·OAuth2·OpenFeign·Cloud LoadBalancer·Circuit Breaker·Spring Batch (`batch`/`-jdbc`/`-data-mongodb`) — ОТЛОЖЕНО, convention plugins готовы в `build-logic/`, не применены ни в одном модуле
- `bff/`·`thymeleaf/`·`sharing/`·`crud/` — НЕ СОЗДАНО

**Checkstyle — порядок импортов** (см. «Активная нить» → «2026-08-20, explicit import groups» — группы теперь `java,javax,com.example,jakarta.persistence,org.assertj,org.jspecify,org.junit,org.testcontainers,reactor.core,*,org.springframework`, пустая строка между КАЖДОЙ, не только вокруг `org.springframework`).

---

## Архитектура и структура проекта

**Многомодульная сборка (Gradle)** — схема: путь — назначение:
- `build-logic/settings.gradle.kts` — `include(":convention")`; included build, подключён через `pluginManagement { includeBuild(...) }` в корневом `settings.gradle.kts`
- `build-logic/convention/.../com.example.{name}.gradle.kts` — convention plugins (Kotlin DSL), namespaced id без суффикса `-conventions` (`com.example.spring-boot`, не `-conventions`); полный граф родитель→потомок всех 49 — см. `docs/convention-plugins-graph.md`
- `settings.gradle.kts` — единственный, в корне; `include(":...")` на все subprojects
- `{service}/{module}/build.gradle.kts` — применяет plugin через `id("com.example.{name}")`
- `{service}/{application,domain,persistence,presentation}/{module}/build.gradle.kts` — для `note`/`user`/`user-note` физическое расположение модуля вложено в подпапку по слою (2026-08-01, было плоско `{service}/{module}/`; см. «Именование» → «Группировка модулей по слою в подпапку» и «Открытые решения»/decisions-log за 2026-08-01). Gradle project path остаётся плоским (`:{service}:{module}`, без вложенности) — `settings.gradle.kts` переопределяет `project(":{service}:{module}").projectDir` на новый физический путь; typesafe accessors (`projects.note.domain`, `projects.userNote.dataJpa` и т. п.) не меняются

**Пакеты по сервисам** — путь — package:
- `note/` → `com.example.note`
- `user/` → `com.example.user`
- `user-note/` → `com.example.usernote` (не `user.note`!)

**Слои внутри сервиса** (зависимость → куда смотрит; физические пути — для `note`/`user`/`user-note`, сгруппированы по подпапке слоя с 2026-08-01) — схема: путь — роль — зависимость:
- `domain/domain/` — records·enums·exceptions — чистая Java, без зависимостей на инфраструктуру
- `domain/contract/` — sync port interfaces → `domain/domain/` (api)
- `domain/contract-reactive/` — reactive port interfaces → `domain/domain/` (api)·reactor-core (implementation)
- `presentation/webmvc/` — driving adapter sync → `domain/contract/`
- `presentation/webflux/` — driving adapter reactive → `domain/contract-reactive/`
- `persistence/data-jpa/` — driven adapter JPA → `domain/contract/`
- `persistence/data-jdbc/` — driven adapter JDBC → `domain/contract/`
- `persistence/data-r2dbc/` — driven adapter R2DBC → `domain/contract-reactive/`
- `persistence/data-mongodb/` — driven adapter MongoDB → `domain/contract/`
- `persistence/data-mongodb-reactive/` — driven adapter Mongo reactive → `domain/contract-reactive/`
- `application/application-{h2,mysql,postgresql}-jpa/` — composition root (sync) → presentation/webmvc + persistence/data-jpa
- `application/application-{h2,mysql,postgresql}-jdbc/` — composition root (sync) → presentation/webmvc + persistence/data-jdbc
- `application/application-{h2,mysql,postgresql}-r2dbc/` — composition root (reactive) → presentation/webflux + persistence/data-r2dbc
- `application/application-mongodb/` — composition root (sync) → presentation/webmvc + persistence/data-mongodb
- `application/application-mongodb-reactive/` — composition root (reactive) → presentation/webflux + persistence/data-mongodb-reactive

Ровно один driven-адаптер и ровно один вендорный БД-драйвер на classpath каждого composition root — обе оси (технология адаптера, вендор БД для jpa/jdbc/r2dbc) выражены границей Gradle-модуля, не рантайм-профилем (пересмотр 2026-07-24 — было `spring.profiles.active`, см. `docs/decisions-log.md` → «Архитектура»: профиль выбирает bean в рантайме, но не убирает лишние драйверы из паковки). 11 composition-root модулей на сервис (9 реляционных + 2 Mongo, у Mongo вендорной оси нет) × 3 сервиса = 33. **⚠️ `user-note/` сознательно отступает от этого абзаца с 2026-08-19** (не WIP — см. «Задачи» → «Продолжение 2026-08-19», «Апдейт 2026-08-19, ещё позже») — вендорная ось для JDBC/JPA/R2DBC снова совмещена в одном модуле на технологию (3 драйвера на classpath, driver bloat принят сознательно как меньшее зло по сравнению со сложностью shared test-fixtures); не постоянное решение — пользователь допускает возврат к разделению по вендору специально ради driver bloat в будущем. `note`/`user` физически не тронуты (33-модульная структура сохраняется) — перенос этого выбора на них не вопрос для обсуждения, станет виден по факту правки пользователем.

**Внутренняя структура driven-адаптера** (пример `data-jpa/`; одинаково для всех технологий) — пакет — классы:
- `com.example.note.data.jpa.adapter` — `NoteAddJpaAdapter`, ...
- `com.example.note.data.jpa.model` — `NoteEntity`
- `com.example.note.data.jpa.mapper` — `NoteJpaMapperContract`, `NoteJpaMapper`
- `com.example.note.data.jpa.repository` — `NoteJpaRepository`

Пакет `model/` используется во всех технологиях (внутреннее имя класса — `Entity`/`{Tech}Entity` для JPA/R2DBC, `Document`/`ReactiveDocument` для MongoDB — см. «Именование»). Driving-адаптеры (`webmvc/`, `webflux/`) организованы плоско, без вложенных пакетов.

---

## Именование

Формат каждой строки: **элемент** — паттерн — пример.

- **Sync base port interface** — `{Entity}Interface` — `NoteInterface`
- **Sync service port interface** — `{Entity}ServiceInterface` (наследник `{Entity}Interface`) — `NoteServiceInterface`
- **Sync controller port interface** — `{Entity}ControllerInterface` — `NoteControllerInterface`
- **Reactive base port interface** — `{Entity}ReactiveInterface` — `NoteReactiveInterface`
- **Reactive service port interface** — `{Entity}ServiceReactiveInterface` — `NoteServiceReactiveInterface`
- **Reactive controller port interface** — `{Entity}ControllerReactiveInterface` — `NoteControllerReactiveInterface`
- **Adapter impl (sync и reactive)** — голый `{Entity}Service` в пакете своей технологии, без суффикса технологии в имени класса — `com.example.note.data.jpa.adapter.NoteService`, `com.example.note.data.r2dbc.adapter.NoteService` и т. д.
- **Controller** — `{Entity}Controller` — `NoteController`
- **Mapper interface** — `{Entity}{Tech}MapperContract` — `NoteJpaMapperContract`
- **Mapper impl** — `{Entity}{Tech}Mapper` — `NoteJpaMapper`
- **Spring Data repo** — `{Entity}{Tech}Repository` — `NoteJpaRepository`
- **JPA model class** — `{Entity}Entity` — `NoteEntity`
- **R2DBC model class** — `{Entity}{Tech}Entity` — `NoteR2dbcEntity`
- **MongoDB model class** — `{Entity}Document` — `NoteDocument`
- **MongoDB reactive model class** — `{Entity}ReactiveDocument` — `NoteReactiveDocument`
- **Общий маркер-интерфейс модели сущности** (новое, 2026-07-14) — `{Entity}Persistable`, в `domain/` — `NotePersistable`, `UserPersistable`, `UserNotePersistable`; реализуют все 5 технологических model-классов сущности сразу (единственный общий предок sync+reactive). Осознанно выбрано имя, пересекающееся с реальным `org.springframework.data.domain.Persistable<ID>` — не конфликтует (префикс сущности делает simple name другим), но напоминает: сам маркер в `domain/` остаётся чистым Java без зависимости на `spring-data-commons`, а НАСТОЯЩИЙ `Persistable<ID>` (если понадобится `isNew()`) реализуется отдельно в каждом адаптере — см. `docs/decisions-log.md` → «Архитектура»
- **Composition-root главный класс** (2026-07-23, расширено вендорным префиксом 2026-07-24, порядок частей имени/модуля перевёрнут на «вендор-технология» в тот же день) — `{Service}{Tech}Application` в `application-{tech}/` для Mongo (`NoteMongoApplication`, `NoteMongoReactiveApplication`); `{Service}{Vendor}{Tech}Application` в `application-{vendor}-{tech}/` для jpa/jdbc/r2dbc (`NoteH2JpaApplication`, `NoteMysqlJpaApplication`, `NotePostgresqlJpaApplication`, ...) — вендор всегда явный префикс перед технологией, без неявного «без префикса = H2» (см. `docs/decisions-log.md` → «Вендор БД — граница Gradle-модуля»). Тестовый класс — `...ApplicationTests` по тому же паттерну. До 2026-07-23 было 2 модуля на сервис (`application`/`application-reactive`) без Tech-суффикса

**Tech**: `Jpa` · `Mongo` · `Jdbc` · `R2dbc` · `MongoReactive` (в имени класса — только для `{Tech}MapperContract`/`{Tech}Mapper`/`{Tech}Repository`/`{Tech}Entity`; адаптеры и порт-интерфейсы суффикс технологии в имени не несут — технология различается пакетом/модулем)

**Vendor** (2026-07-24, только для composition-root класса/модуля jpa/jdbc/r2dbc, см. «Composition-root главный класс» выше): `H2` · `MySQL` · `PostgreSQL` — всегда префикс перед `{Tech}`, порядок `{Vendor}{Tech}` (`H2Jpa`, не `JpaH2`) — пересмотрено в тот же день: изначально был выбран порядок `{Tech}{Vendor}` («технология доступа архитектурно более фундаментальна»), затем перевёрнут по решению пользователя без изменения обоснования самой оси (вендор/технология остаются раздельными осями именования, поменялся только порядок отображения). **Вопрос снят как неактуальный 2026-08-19** — модуль `{Tech}-{Vendor}` физически перестал существовать (5 плоских модулей вместо 11 вложенных вендорных, см. «Задачи» → «Продолжение 2026-08-19» п. 2), сравнивать порядок `{Vendor}{Tech}` vs `{Tech}{Vendor}` больше не с чем; если вендорная ось модуля когда-нибудь вернётся — вопрос открыть заново, не считать прежнее наблюдение решением

**Группировка модулей по слою в подпапку** (2026-08-01, начато пользователем вручную в `user-note/` — `git mv` в новую вложенность, опечатка `pestistence` вместо `persistence`; агент исправил опечатку и применил ту же структуру симметрично к `note/`·`user/`): для всех трёх зрелых сервисов физический путь листового модуля — `{service}/{layer}/{module}/`, где `{layer}` ∈ `application`·`domain`·`persistence`·`presentation` (`domain/domain`, `domain/contract`, `domain/contract-reactive` — все три под `domain/`; `data-*` → `persistence/data-*`; `webmvc`/`webflux` → `presentation/{module}`; все `application-*` → `application/application-*`). Сам Gradle project path (и, следовательно, имя typesafe-accessor) не меняется — остаётся плоским `:{service}:{module}` — только физическая директория переопределена через `project(":{service}:{module}").projectDir` в `settings.gradle.kts` (см. «Архитектура и структура проекта» → «Многомодульная сборка»). Ранее аналогичный, но более узкий вопрос («каталог `data/` на уровне сервиса» — только для `data-*`) обсуждался и было решено оставить плоско (цена реорганизации выше пользы) — это решение пересмотрено данным изменением, которое к тому же шире исходного вопроса (охватывает все 4 слоя, не только `data-*`)

---

## Стек

Формат каждой строки: **инструмент** — версия.

- **Java** — 21 (было 25 с 2026-07-13 по 2026-08-01, откат на 21 — сознательное решение пользователя 2026-08-01, без развёрнутой причины на момент записи; обе LTS)
- **Gradle** — 9.7.1 (было 9.6.1 в этой строке — документация не поспевала за фактическим состоянием, `gradle-wrapper.properties` уже был на 9.7.0; см. «Задачи» → «Активная нить» → «2026-08-29» про потерю `distributionSha256Sum` при обновлении до 9.7.1)
- **Spring Boot** — 4.1.0 (было 4.0.6 до 2026-07-13)
- **Spring Cloud** — 2025.1.2 (уже последняя, совместима и с Boot 4.1.0 — проверено 2026-07-13)

**Spring Boot 4 — критичные отличия от Boot 3** (источник: существующие `build.gradle.kts` в проекте; документация docs.spring.io местами показывает Boot 3 и не заслуживает доверия — сверяться с файлами проекта):

- `starter-web` → `starter-webmvc`; `starter-aop` → `starter-aspectj`
- `@MockBean`/`@SpyBean` → `@MockitoBean`/`@MockitoSpyBean`
- OAuth2-стартеры: `oauth2-resource-server` → `security-oauth2-resource-server`; аналогично для `client` и `authorization-server`
- Spring Authorization Server — часть Spring Security 7, отдельной версии не имеет
- `@WebMvcTest`, `@DataJpaTest` — в отдельных `*-test` стартерах, не в `starter-test`
- `@SpringBootTest` не подключает MockMvc/WebClient автоматически — нужен `@AutoConfigureMockMvc`
- Jackson 3: пакет `tools.jackson` (кроме `jackson-annotations` — остался на `com.fasterxml`)
- MongoDB: весь префикс свойств `spring.data.mongodb.*` → `spring.mongodb.*` (deprecation level=error, since 4.0.0; connection-слой вынесен в отдельный модуль `spring-boot-mongodb` с `MongoConnectionDetails`, `MongoTemplate` строит `spring-boot-data-mongodb`) — см. «Активная нить» → «2026-08-29, `spring.data.mongodb.*` → `spring.mongodb.*`»

---

## Технологии

Глоссарий технологий за пределами Spring-стека (в основном ещё не используются) вынесен в `docs/tech-glossary.md` — не нужен на каждую сессию, читать при работе с конкретной технологией из списка (Kubernetes/Terraform/Jenkins/SonarQube/AWS/Redis/Kafka/Elastic/OAuth2/jMolecules/Axon).

---

## Принятые решения

Полная хронология архитектурных решений (Architecture/HTTP/Ошибки/Маппинг/Reactive-семантика/Convention plugins/Синхронизация версий/Стиль кода) вынесена в `docs/decisions-log.md` — исторический лог, нужен редко (обычно только при пересмотре конкретного решения, grep по дате/теме). Статус «принято» не закрывает вопрос навсегда — см. «Правила» → «Пересмотр решений».

---

## Открытые решения

- **Единый источник истины для версий (Docker-образы, Java) во всех окружениях — dev/test/prod/GitHub Actions/Kubernetes** (2026-08-21, вырос из узкого вопроса «версия Docker-образа для Testcontainers vs реальный запуск», см. «Задачи» → «Активная нить» → «2026-08-20»/«2026-08-21» — там же техническое решение конкретно для `JDBC`-модуля; здесь — вопрос шире, по всему проекту и всем окружениям; **решение — в следующей сессии, здесь только исследование**) — главная находка: **для Java эта проблема уже решена в репозитории**, и решение — рабочий образец для Docker. Корневой `.java-version` (значение `21`) читают одновременно (1) Gradle — `build-logic/convention/.../com.example.java.gradle.kts:20`, `providers.fileContents(rootProject.layout.projectDirectory.file(".java-version")).asText.get().trim().toInt()` → `JavaLanguageVersion.of(...)`; (2) `.github/workflows/gradle.yml` — `actions/setup-java@v4` c `java-version-file: '.java-version'` (подтверждено официальной документацией `actions/setup-java` — параметр поддерживает файлы `.java-version`/`.tool-versions`/`.sdkmanrc`). Один файл, два независимых читателя, ни одной второй копии числа `21` в репозитории — то есть по оси «Java» единый источник истины уже есть и не требует решения.
  По оси **Docker-образов** (`mysql:9`/`postgres:18`/…) готового моста между инструментами нет, и это факт устройства экосистемы, не недосмотр: Docker Compose умеет **нативно** интерполировать `${VAR}` в `image:` из project-level `.env`-файла (официальная документация Docker Compose — `.env` подставляется до парсинга самого compose-файла); Testcontainers-Java — нет, `MySQLContainer`/`PostgreSQLContainer` принимают тег только явным Java-аргументом конструктора, единственный официальный конфигурационный крюк — `testcontainers.properties`+`ImageNameSubstitutor` (полная подмена образа, не выбор версии) — точечное переопределение тега через конфиг **официально не рекомендовано и deprecated** (`java.testcontainers.org/features/configuration`); GitHub Actions читает версию сама только если использует `services:`-контейнеры — в этом проекте Testcontainers всегда стартует изнутри Gradle `test`-таска на раннере, отдельного `services:`-блока нет, значит ось GitHub Actions для Docker-образов сегодня не требует отдельного моста вообще, она уже наследует то, что получит Gradle; Kubernetes в проекте не используется совсем (см. «Технологии» → `docs/tech-glossary.md`) — для него нет ни одного файла, с которым сверяться, вопрос сегодня чисто гипотетический, готовый паттерн на будущее — Kustomize `images:`-трансформер или Helm `values.yaml` (централизация тега на уровне манифеста, независимая проверка через `kubernetes-sigs/kustomize` доку).
  Рассмотренные варианты для Docker-оси: (а) **зеркалировать `.java-version`-паттерн** — общий текстовый файл (например корневой `.env` или `docker-versions.properties`) с одной строкой на вендор, Gradle читает его тем же `providers.fileContents(...)`, что уже используется для Java, и прокидывает как `systemProperty`/`environment` в `test`-таск, Testcontainers-код читает `System.getProperty(...)` вместо литерала; `compose.yaml` (если появится) читает тот же `.env` нативной интерполяцией Compose — совпадает по механике с уже проверенным Java-решением, не изобретает новый инструмент. (б) **Gradle version catalog (`libs.versions.toml`)** — рассмотрен и отклонён для этой конкретной оси: официальная позиция Gradle («Best Practices for Dependencies») — catalog предназначен для версий зависимостей/плагинов, использование `[versions]` как общего хранилища произвольных строк (образ БД — не Gradle-зависимость) прямо не рекомендовано самим Gradle; остаётся легитимным для смежного, но другого вопроса — версий Java-библиотек (`mysql-connector-j`, testcontainers-модули и т. п.), не тегов образов. (в) **Renovate `customManagers`/`regexManagers` с группировкой PR** — не устраняет дублирование строк, а автоматически держит все совпадения (в т. ч. Java-литерал и `compose.yaml`) синхронно забамленными через один PR по регулярке — дополняет (а), не заменяет: (а) устраняет саму необходимость двух копий, Renovate нужен, только если копии всё равно остаются (например тег в `compose.yaml` и версия `mysql-connector-j` — два разных, но связанных числа, которые физически нельзя слить в одну строку). (г) **mise/asdf `.tool-versions`** — рассмотрен и отклонён как нерелевантный для Docker-оси: управляет версиями устанавливаемых локально CLI-инструментов (Java, Node и т. п. — тем же, что уже решает `.java-version`+`setup-java`), не версиями тегов образов внешних сервисов, которые не устанавливаются, а пуллятся.
  **Рекомендация для следующей сессии**: вариант (а), как прямое расширение уже проверенного и работающего в этом репозитории паттерна — не нового изобретения; (в) держать в уме как дополнение, когда/если появится второй легитимно-раздельный источник той же версии (например Java-драйвер БД). Ничего не реализовано — только сбор фактов, ждёт решения пользователя
- **`com.example.codequality-jacoco-report-aggregation` — настройка верна, агрегировать пока нечего** (2026-08-15, см. «Задачи» → «Завершено» → «золотой стандарт») — плагин применён на каждом модуле через `com.example.java`; отчёт сейчас `SKIPPED`/пустой не из-за ошибки конфигурации, а потому что: (1) composition-root модули пока пустой skeleton без единой `project()`-зависимости, (2) нигде нет реальных тестов, производящих jacoco-данные. `jacocoAggregation` не нужно объявлять явно при наличии `jvm-test-suite` (есть у нас через `java`) — сверено с первоисточником (`jacoco_report_aggregation_plugin.adoc`, `github.com/gradle/gradle`). Включится сам, как только composition-root-модуль получит `implementation(projects.*)` и появятся тесты — отчёт в `build/reports/jacoco/testCodeCoverageReport/html/index.html` этого модуля. Ничего в build-логике менять не нужно
- **Качественная настройка Checkstyle/codequality под командную разработку** (2026-08-01, приоритеты: соответствие официальной документации + работает «из коробки» одинаково для всех при клонировании) — 4 подпункта: (1) **не реализовано** — независимость от ОС (line endings/path separator/encoding): `.gitattributes` заморожен на шаблоне Initializr, реального решения для чужого `core.autocrlf` на checkout нет; (2) **реализовано частично** — базовое форматирование теперь IDE-независимо через `.editorconfig` (2026-08-15), «порядок методов = порядок в интерфейсе» не покрыт (см. следующий пункт); (3) **реализовано** — `foojay-resolver-convention` (автозагрузка JDK) + `distributionSha256Sum` (защита дистрибутива Gradle); (4) **пересмотрено 2026-09-04** — explicit-группы `ImportOrderCheck` и Spotless-автофикс-на-`compileJava` сняты по прямому запросу «всё по дефолту»; порядок импортов теперь держит `google-java-format` (через `googleJavaFormat().aosp()`) само по себе плюс `CustomImportOrder` из `google_checks.xml` — вопрос «генерация списка групп без ручного перечисления» снят как неактуальный, explicit-список больше не существует
- **Порядок методов реализации = порядок объявления в интерфейсе** (2026-08-08) — IntelliJ IDEA умеет это из коробки (Code Style → Java → Arrangement → Grouping Rules → «by order in super class/interface» + Actions on Save → «Rearrange code»), но это настройка IDE-профиля, не build-гейт — не защищает от другого редактора/выключенной опции, переносится на команду только через коммит `.idea/codeStyles/Project.xml`. Build-time гейт (fail на несоответствие) — готового инструмента нет, 2 кандидата: (1) кастомный Error Prone `BugChecker` (в проекте уже есть `net.ltgt.errorprone`, потенциально с `SuggestedFix`-автофиксом через `-XepPatchChecks`); (2) JUnit-тест на `com.github.javaparser:javaparser-core`, сравнивающий порядок методов интерфейс/реализация. `SpringMethodOrderCheck` (Checkstyle) эту задачу не решает — проверяет только `equals→hashCode→toString`. Не реализовывать до отдельного запроса
- **Симметрия ArchUnit + стратегия покрытия матрицы адаптер×драйвер тестами** (2026-07-23; **не реализовывать до отдельного запроса**) — два вопроса:
  1. ArchUnit нигде не подключён (упомянут только в `docs/tech-glossary.md`). Кандидат на автоматизацию правил, сегодня проверяемых вручную грепом (расслоение hexagonal, запрет plugin id/зависимостей в обход `com.example.*`). Технический риск, не исследован: ArchUnit сканирует скомпилированные классы через reflection — в multi-module Gradle это либо один агрегирующий тестовый модуль с зависимостью на все 39 модулей (широкое схождение путей, качественно отличное от разобранных диамантов), либо отдельный Gradle-таск, компонующий classpath вручную
  2. Матрица тестового покрытия — вариант (A) «отдельный класс/модуль на комбинацию» реализован для `note`/`user` (33 composition-root модуля, один `*ApplicationTests` на модуль); **для `user-note` этот инвариант больше не действует с 2026-08-19** (5 объединённых модулей, вендор внутри — см. «Задачи» → «Активная нить»). Вариант (B) `@ParameterizedTest` снят как противоречащий тогдашнему «вендор — граница модуля»; вариант (C) — мета-тест по типу ArchUnit («на каждый вендорный плагин должен существовать application-модуль») — зависит от решения по пункту 1, актуальность для `user-note` пересмотреть заново после изменения 2026-08-19
- **Поведенческая аналогичность адаптеров/драйверов** (названо пользователем главной проблемой на пути к зрелости CRUD-модулей, 2026-07-24; **не реализовывать до отдельного запроса**) — реализации порта по матрице технология×вендор должны вести себя одинаково при одинаковом сценарии; находки по факту чтения кода `note/` (не перепроверялись отдельно для `user/`/`user-note/`, предполагается та же структура):
  1. Порты не специфицируют поведение (голый `Object`, без `throws`/`Optional`/`@Nullable`/Javadoc) — **2026-08-18 переосмыслено как осознанное разделение**: спецификация поведения сознательно живёт в тестах application-уровня, порт остаётся чисто структурным контрактом
  2. Обработка «не найдено» сегодня случайно одинакова во всех 5 адаптерах (`orElseThrow(NoteNotFoundException)`/`switchIfEmpty(Mono.error(...))`) — не гарантирована ни портом, ни тестом
  3. Реальные расхождения: генерация ID тремя механизмами (JPA — `@GeneratedValue` через Hibernate; JDBC/R2DBC — `DEFAULT RANDOM_UUID()` в `schema.sql`; MongoDB — `UUID.randomUUID()` в мапере); duplicate-key нигде не транслируется ни в одном из 5 адаптеров (сценарий сегодня недостижим — нет unique-constraint кроме PK — но латентный риск, `NoteExceptionHandler` обработает только `NoteNotFoundException`); MongoDB (blocking) `add()` использует `mongoTemplate.insert()` напрямую, минуя repository, в отличие от остальных операций/адаптеров
  4. Одинаковые (не расходящиеся) пробелы во всех 5: `@Transactional` отсутствует везде (`merge()`/`replace()` — read-then-write без атомарности); Bean Validation отсутствует везде (аннотации `@NotNull`/`@NotBlank` на model-классах не расставлены, хотя `spring-boot-starter-validation` подключён точечно)
  5. **Referential integrity между `user-note/` и `user/`·`note/` не проверяется вообще** (найдено 2026-07-24) — `create()` в `UserNoteService` (`data-jpa`) сохраняет `UserNoteEntity` без проверки, что `userId`/`noteId` реально существуют в `user/`/`note/`: ни синхронного вызова порта, ни события, ни constraint (сервисы физически разделены). Там, где проверка вроде есть (`existsByUserId` — проверяет «есть ли уже связка в `user_note`», не «существует ли пользователь»), она обманчива — `UserNotFoundException` там означает «у userId нет заметок», не «пользователя нет». Не перепроверено для jdbc/r2dbc/mongodb/mongodb-reactive `user-note/`. Тот же класс проблемы («нет referential integrity через границу сервиса») встанет и в «Регистрация auth/ ↔ user/» ниже

  Кандидат-решение, не проработан: общий conformance test suite через `java-test-fixtures` — абстрактный JUnit-класс против интерфейса порта, параметризуемый в каждом `data-*`/composition-root модуле своим адаптером; требует заранее провести границу «что обязано совпадать» vs «что законно различается» (пример законного различия — bean-validation автотриггер: Hibernate триггерит, MongoDB/R2DBC/JDBC — нет)
- **Стоимость масштабирования composition-root матрицы** (2026-07-24) — рост числа модулей мультипликативен (сервисы × технологии × вендоры). Два разных вопроса: (1) рост числа модулей — цена уже принятого решения «вендор — граница модуля» (для `note`/`user`; `user-note` отступил от этого с 2026-08-19, см. «Задачи»), откат вернул бы driver bloat; (2) рост стоимости добавления модуля (дублирующийся boilerplate) — можно снижать без изменения топологии, варианты не проработаны (пересмотреть обязательность полной 3×3-симметрии; оценить реальный объём дублирования перед решением, что менять). Эмпирика по времени сборки — см. «Правила» → «Build» → «`clean check` с реальным Docker» (MySQL ~2× медленнее PostgreSQL/MongoDB, H2 без контейнера 9–14с). Не реализовывать до отдельного запроса
- **Область подключения `spring-boot-starter-validation`** — решено 2026-08-01 (атомарный `spring-boot-validation`, только `webmvc`/`webflux`/`data-jpa` × 3 сервиса). Остаточный пункт, **не поднимать и не напоминать до отдельного запроса**: `data-mongodb`/`data-mongodb-reactive` требуют написать и зарегистрировать `ValidatingMongoEventListener`+`LocalValidatorFactoryBean` (Spring Boot не автоконфигурирует), без этого голый стартер там ничего не даёт. R2DBC/JDBC (Spring Data Relational) — ни автотриггера, ни listener-механизма нет вообще, подключать нечего
- **Регистрация `auth/` ↔ `user/`** — Lazy / Sync / Events (Kafka); та же проблема класса «нет referential integrity через границу сервиса», уже реально найденная между `user-note/` и `user/`·`note/` — см. «Поведенческая аналогичность» → п. 5 выше
- **Возврат мутирующего use case** — DTO vs `void`, не проработано
- **`@Transactional` на методах адаптера** — решение зафиксировано, но не реализовано ни в одном адаптере ни одной технологии (~150 файлов). Решить: реализовать по всем адаптерам или снять как устаревшее (Spring Data репозитории и так транзакционны на уровне метода)
- **`user/`: `findByEmail`/`findByUsername` без HTTP-входа** — доведены до всех driven-адаптеров, не выведены в `webmvc`/`webflux`. Варианты: задел под будущий `auth/`, добавить контроллеры сейчас, или убрать как неиспользуемое
- **Javadoc в `package-info.java`** — ни один пакет не содержит package-level Javadoc, только `@NullMarked`; `checkstyle.xml` сейчас исключает javadoc-проверки — включение потребует такого комментария в каждом пакете
- **Composite build на границе сервисов** — каждый сервис мог бы подключаться через `includeBuild(...)` вместо `include(...)`. Ни один сервис не ссылается на модули другого, accessors не пострадали бы, но цена (7 новых `settings.gradle.kts`, ручная агрегация `build`/`check`/`clean`) больше выигрыша без текущего драйвера (независимое CI/версионирование, разъезд по репозиториям)
- **Инструмент миграции БД по адаптерам** (2026-07-15) — справочный обзор кандидатов (Flyway/Liquibase, Mongock/Flamingock, elasticsearch-evolution) вынесен в `docs/db-migration-tools-reference.md`. Ключевая находка: Mongock в maintenance mode, EOL конец 2026, преемник — Flamingock. Выбор не сделан — не реализовывать до отдельного запроса
- **Полная модель Google Docs (совместное редактирование)** (2026-07-23) — набросок требований вынесен в `docs/google-docs-full-model.md`. Рекомендация — не проектировать целиком: начать с обычного CRUD-документа по паттерну `note/`, real-time collaborative editing выделить в отдельную задачу. Не реализовывать до отдельного запроса

---

Правила ведения `docs/file-catalog.md`/`docs/spring-boot-starters-reference.md`/`docs/decisions-log.md`/`docs/tech-glossary.md` (что можно/нельзя менять по инициативе агента) — см. «Правила» → «Лимит размера файла» выше. Сами файлы и их назначение — см. «Справочные файлы репозитория» в начале этого файла.
