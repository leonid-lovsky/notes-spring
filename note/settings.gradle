pluginManagement {
    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'note'

include ':application'
include ':webmvc'
include ':data-jpa'
