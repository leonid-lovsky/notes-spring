pluginManagement {
    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'user-note'

include ':application'
include ':webmvc'
include ':data-jpa'
