pluginManagement {
    includeBuild 'build-logic'
}

includeBuild 'application'

includeBuild 'auth'

includeBuild 'build-logic'

includeBuild 'crud'

includeBuild 'note'

includeBuild 'user'

includeBuild 'user-note'
